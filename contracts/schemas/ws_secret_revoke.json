{
  "additionalProperties": false,
  "description": "``secret-revoke``: client -> server, SOFT-revoke one secret.\nThe vault entry is not deleted, so the audit trail survives and the user can\nun-revoke without re-entering the key; lookups filter revoked keys out.",
  "properties": {
    "envelope_type": {
      "const": "secret-revoke",
      "default": "secret-revoke",
      "title": "Envelope Type",
      "type": "string"
    },
    "secret_id": {
      "title": "Secret Id",
      "type": "string"
    }
  },
  "required": [
    "secret_id"
  ],
  "title": "SecretRevokeEnvelopePayload",
  "type": "object"
}
