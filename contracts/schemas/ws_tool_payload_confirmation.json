{
  "additionalProperties": false,
  "description": "``tool-payload-confirmation``: the reply that authorizes a paused gate.\n``warning_id`` selects the paused dispatch; the decision then proceeds with\nthe original or the revised args, or surfaces a cancellation.",
  "properties": {
    "decision": {
      "enum": [
        "proceed",
        "cancel",
        "narrow_scope"
      ],
      "title": "Decision",
      "type": "string"
    },
    "envelope_type": {
      "const": "tool-payload-confirmation",
      "default": "tool-payload-confirmation",
      "title": "Envelope Type",
      "type": "string"
    },
    "revised_args": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Revised Args"
    },
    "warning_id": {
      "title": "Warning Id",
      "type": "string"
    }
  },
  "required": [
    "warning_id",
    "decision"
  ],
  "title": "PayloadConfirmationEnvelopePayload",
  "type": "object"
}
