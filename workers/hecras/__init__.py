"""HEC-RAS 6.x Linux computation-engine worker (mesh wave M3).

Carries HEC's official public-domain 6.6 Linux engines (RasGeomPreprocess /
RasUnsteady / RasSteady) so the geometry preprocess + unsteady solve run
headless. M3 proves the geometry pipeline on HEC's shipped Muncie test project;
the full HEC-RAS engine landing (registered tool / template / contracts) is a
later wave.
"""
