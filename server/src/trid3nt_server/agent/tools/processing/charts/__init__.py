"""Chart-emission processing tools (Vega-Lite interactive charts).

Histogram / time-series / damage-distribution / choropleth-legend tools
regrouped here (cull pass 2). Tool names + registry
metadata are unchanged; only the import path gains the ``charts`` segment.
"""
