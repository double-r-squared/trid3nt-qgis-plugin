# Template routing phrasings (door dissolution, ADR 0094): walked into the main
# retrieval index directly -- the engine templates are ordinary retrieval-pool tools.
geoclaw_inundation:
- run the GeoClaw adaptive-mesh shallow-water inundation solve for this AOI
- tsunami run-up depth + arrival animation along this stretch of coast
- dam-break / levee-failure flood wave routed over the terrain below
- storm-surge run-up over the topography with adaptive mesh refinement
- peak overland depth from a magnitude-8 offshore earthquake source
- how far inland does the tsunami reach and what is the max depth here
- trace the wake / drift path of particles carried by the tsunami in the harbour
- seed Lagrangian drifter gauges and follow where the flow advects them
- track how far floating particles are pushed by the wave over time
- restrict the fgmax run-up maxima to the onshore cells of the real DEM
- record max wave height and first-arrival time only on the dry-land cells
- give me a smooth fluid tsunami run-up animation instead of the choppy AMR frames
- dump evenly-spaced fixed-grid output frames for a smooth inundation scrubber
- render the wave run-up as smooth single-resolution animation frames over the AOI
