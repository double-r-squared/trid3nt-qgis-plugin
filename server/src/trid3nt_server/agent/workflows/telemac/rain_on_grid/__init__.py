"""TELEMAC-2D rain-on-grid (rainfall-runoff) template package.

Homes the ADR 0195 rain-on-grid foundation: SCS curve-number infiltration
preprocessing (``cn_infiltration``). The registered ``telemac_rain_on_grid``
template body lands in a follow-on wave (worker deck + parser bump + image
rebuild + live Coweeta proof); this package is the offline-testable substrate
it builds on.
"""
