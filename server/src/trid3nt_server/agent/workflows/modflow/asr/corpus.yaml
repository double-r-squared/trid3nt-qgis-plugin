# TEMPLATE tier (engine-door refactor): phrasings for modflow_asr. NOT merged
# into the main retrieval index (templates are excluded from the default
# pool); the run_modflow door surfaces this template via gate expansion.
# Carried verbatim from the migrated run_model_asr_scenario corpus entries.
modflow_asr:
- aquifer storage and recovery cycle simulation
- how much injected water can we recover from the ASR well
- ASR injection storage recovery scenario
- store treated water in the aquifer and recover it later
- recovery efficiency of an ASR scheme at this site
