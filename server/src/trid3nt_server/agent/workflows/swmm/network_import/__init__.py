"""``swmm_network_import`` engine template package (dual-drainage MINOR system).

Imports a REAL municipal storm-drain GIS network (nodes + conduits) into a
runnable SWMM model - the practice-verification's #1-ranked gap over the
DEM-synthesized overland mesh. See ``network_import.py``.
"""
