# Engine-door corpus (DOOR tier - walked into the main index; the door competes
# in per-turn retrieval alongside general tools). Hazard-common landslide /
# overland-flow phrasings route to the run_landlab door, whose gate expansion
# then surfaces the specific landlab_* template (select-then-call).
# Template-specific phrasings live in each workflows/landlab/<template>/corpus.yaml
# (TEMPLATE tier - NOT merged into this index). Carried from the migrated
# run_landlab_susceptibility corpus entries.
run_landlab:
- map where the slopes are likely to fail on this hillside after heavy rain
- model landslide susceptibility and factor of safety across this catchment
- where is slope instability highest here, give me a probability of failure raster
- give me a slope-stability hazard map for this mountainous AOI
- which hillslopes are prone to slumps or shallow landslides here
- route a rainfall pulse over the DEM and show me the peak surface runoff depth
- compute overland flow / surface runoff from a storm across this watershed
- what landslide or overland-flow models can you run
- which Landlab surface-process scenario fits my question
