# Readability ledger

Comments and docstrings that a better NAME or a small EXTRACTION would make
unnecessary. The docs and docstring wave RECORDS these and applies NONE of them:
renaming or extracting inside a function is a behavior surface, reviewed as code
by whichever wave owns the module.

| file | line | the comment as it stands | the change that would remove it | risk |
| --- | --- | --- | --- | --- |
| trid3nt_server/workflows/runtime/data.py | 30 | `_CoversAOI`: "a domain must be BOUND and have an extent ... the artifact's own extent is never read" | rename the sentinel to what it actually tests (`DomainIsBound`), so the class name stops promising a coverage check the docstring has to walk back | rename |
| trid3nt_server/workflows/runtime/data.py | 186 | `#: How a supplied artifact is checked against the domain - BOUND-DOMAIN-ONLY under ``CoversAOI``` | same rename: the field comment exists only to correct the sentinel's name | rename |
| trid3nt_server/workflows/runtime/data.py | 237 | `refuse_wrong_shape`: "Suffix-deep and no deeper; an unclassifiable artifact passes." | the depth is `artifact_class`'s contract, not this function's; a name saying suffix (`class_from_suffix`) would carry it at the call site | rename |
| trid3nt_server/workflows/runtime/journal.py | 139 | `_row`: "One resolved param, WITH its door and basis - the sheet, not just the values." | rename to `_sheet_row`, so the returned shape is named rather than described | rename |
| trid3nt_server/workflows/runtime/ledger.py | 58 | `_stable`: the digest's contribution rules live in `inputs_digest`'s docstring because `_stable` is unnamed for what it does | rename to `_digest_contribution` and the rules read off the name | rename |
| trid3nt_server/workflows/runtime/rerun/reuse.py | 62 | `_node_key`: "A branch marker is not Ref-able and every one shares a placeholder step, so branches are keyed by INDEX" | extract the branch case into `_branch_key(index)`, and the two key spaces stop needing prose to keep apart | extract |
| trid3nt_server/workflows/runtime/interpreter.py | 465 | `_refuse_invented_physics`: the three-clause comment enumerating which mode/emitter/review combinations refuse | extract the predicate as `_has_review_surface(...)`, and the enumeration becomes the function's own body | extract |
| trid3nt_server/workflows/runtime/params.py | 290 | `wire_value`: the six-significant-figures comment block at the float branch | extract `_six_significant_figures(value)`; the constraint moves onto the name and the rounding rule stops being a comment in a dispatch chain | extract |
| trid3nt_server/workflows/shared/cog_io.py | 391 | `gs_fallback_to_file` and `gs_backend`: the comment block explaining that `gs_backend` selects nothing and the branch is only reachable when a caller forces a scheme | the parameter names outlived their backend; renaming to `fallback_to_file` and dropping the inert `gs_backend` removes the comment entirely | restructure |
| trid3nt_server/workflows/solver/solver.py | 285 | `_download_object`: "Dispatch on the URI SCHEME, never on the manifest field name: the input entries are keyed ``gs_uri``" | rename the manifest field to `uri` in the worker contract; the comment exists purely to defuse the stale key name | restructure |
| trid3nt_server/workflows/solver/solver.py | 668 | `input_uri = item["gs_uri"]  # the field NAME only; the value is a uri` | same contract rename | restructure |
| trid3nt_server/workflows/solver/solver.py | 62 | `NFR_P_4_TARGET_SECONDS`: its comment has to say what the constant is a target FOR, the name carrying only a spec label | rename to `PROGRESS_RAMP_TARGET_SECONDS` and the comment collapses to the ramp formula | rename |
| trid3nt_server/workflows/solver/solver.py | 871 | `_try_get_completion_s3`: "``None`` when the object is absent or transiently unreadable ... malformed JSON RAISES" | extract the absent-vs-corrupt discrimination into `_completion_or_none`, so the two outcomes are two named paths | extract |
| trid3nt_server/workflows/runtime/workflow.py | 464 | `_wire_signature`: the comment block explaining that CONSTANT-door params are absent from the signature but still seatable through `**wire` | extract `_seatable_but_unoffered(params)`; the carve-out is currently only discoverable as prose | extract |
| trid3nt_server/workflows/shared/aoi.py | 28 | `location_or_bbox`: the three-behaviour comment block above `_coerce` | extract each branch (`_bbox_that_is_a_place_name`, `_refuse_no_aoi`, `_drop_bbox_for_location`), and the block becomes three names | extract |
| trid3nt_server/workflows/mesh/artifact.py | 34 | `mode: str  # the MESHER that built it: om2d / reg_grid` | rename the field to `mesher`; the comment exists only because `mode` names nothing | rename |
| trid3nt_server/workflows/mesh/inputs.py | 24 | `op_input`: the comment block enumerating the two conversions and the class they are read off | a `_CONVERSION_BY_CLASS = {"raster": op_raster, "vector": op_geometry}` table makes the enumeration the code | restructure |
| trid3nt_server/workflows/mesh/tool.py | 137 | `resolve_mesh`: the "two questions, two owners" comment block above the two refusals | extract `_refuse_supply(art, accepts)`; the pair being asked together at the door becomes one name | extract |
| trid3nt_server/workflows/mesh/gate.py | 337 | `_apply_gate_revision`: `for name in ("resolution_m",)` with the docstring saying which rows move | lift the tuple to `_CARD_PARAMS`, and the movable-row rule reads off the constant | restructure |
| trid3nt_server/workflows/mesh/session.py | 236 | `_display_face`: the comment block saying it memoizes a WRITE and not state | rename to `_display_face_written_once` (or extract `_write_display_face`), and the memo stops needing prose | rename |
| trid3nt_server/workflows/mesh/topology.py | 67 | `read_topology`: the comment block explaining what the `states` sentence is for | extract `_states_sentence(order)`; the sentence gets a name and the block goes | extract |
| trid3nt_server/workflows/mesh/meshers/om2d.py | 152 | `build`: the comment block explaining why everything before the first primitive runs in the generation call | extract `_split_at_first_primitive(post)`; the split's meaning moves onto the name | extract |
| trid3nt_server/workflows/mesh/meshers/om2d.py | 679 | `_emitted`: its docstring exists to say which files it writes | rename to `_write_solver_files` | rename |
| trid3nt_server/workflows/mesh/meshers/reg_grid.py | 51 | `_with_ops`: "Every op a reg_grid recipe can name is a shared primitive running here" | rename to `_run_shared_primitives` | rename |
| trid3nt_server/workflows/mesh/meshers/drivers/om2d_driver.py | 325 | `_Build.edge_length`: "What ``generate_mesh`` is handed: the sizing stack, or the uniform word" | rename to `sizing_for_generate_mesh`; the docstring is the name spelled out | rename |
| trid3nt_server/workflows/mesh/shared/nodes.py | 107 | `sample_raster_at_nodes`: the rim-pixel comment block at the `np.clip` | extract `_clip_to_whole_pixels(xs, ys, src)`; the derivation moves onto the name | extract |
| trid3nt_server/workflows/mesh/shared/primitives.py | 425 | `_runs.run_for`: the "WHICH contour carries the face" comment block | extract `_contour_carrying(face)` | extract |
| trid3nt_server/workflows/mesh/shared/primitives.py | 447 | `_window`: the name says nothing about walking a ring outward within a tolerance | rename to `_run_within_tolerance` | rename |
| trid3nt_server/workflows/mesh/shared/primitives.py | 72 | `set_boundary_roles`: the exempt docstring's four face shapes | a `Face` parser named for the shapes it accepts would carry that half of the contract | restructure |
| trid3nt_server/workflows/mesh/shared/formats/schism_gr3.py | 90 | `extract_boundary_loops`: the Eulerian-decomposition comment block | extract `_consume_boundary_edges(cells)`; the walk's invariant becomes its name | extract |
| trid3nt_server/tools/vector_tiles.py | 66 | `strategy: str  # "simplified" \| "capped" \| "simplified+capped" \| "inline" \| "tiled"` | type the field as a `Literal` of those five; the enumeration moves onto the type and the trailing comment goes | restructure |
| trid3nt_server/tools/vector_tiles.py | 177 | `_strategy_label`: "Dense, but no vertices dropped and no features cut - only coordinate precision was trimmed. Do NOT claim 'simplified'." | the caveat exists because the value is named `"inline"`; naming it `"precision_only"` states what happened and the comment goes | restructure |
| trid3nt_server/tools/payload_sampling.py | 40 | `SampledEstimate`: "`kind` is exactly `\"measured\"` (sampled window) or `\"analytic\"` (fallback model)" | type `kind` as `Literal["measured", "analytic"]`; the contract stops needing prose | restructure |
| trid3nt_server/tools/payload_sampling.py | 17 | `DEFAULT_PX_CAP`: "must stay in lock-step with theirs" | one shared constant both this module and the raster fetchers import; a lock-step comment is what a duplicated constant costs | restructure |
| trid3nt_server/tools/cache.py | 272 | `_read_through_s3`: "any storage failure degrades to fetch-fresh-uncached rather than raising" | extract the degrade path as `_fetch_fresh_uncached(fetch_fn, provenance)`, called from each except arm, and the promise reads off the name | extract |
| trid3nt_server/tools/cache.py | 355 | `read_through`: "`storage_client` is accepted and ignored", and the bare `del storage_client` | drop the parameter and update the call sites; the sentence documents only a dead argument | restructure |
| trid3nt_server/tools/cache.py | 48 | `_canonicalize_params`: "`default=str` is deliberate - an unserializable value gets a stable string form rather than a TypeError" | extract `_stable_json(obj)` and the leniency is the named function's contract, not a note on a dumps call | extract |
| trid3nt_server/tools/tool_arg_normalizer.py | 537 | `coerce_latlon`: "the naive `tuple(float(v) for v in value)` would iterate the string's CHARACTERS and die on the decimal point" | extract `_two_floats_from_string(s)`; the trap is then a property of a named parser with its own test, not a warning inside a branch | extract |
| trid3nt_server/tools/tool_arg_normalizer.py | 815 | `normalize_args`: the block explaining that a combined `spill_location_latlon` string MUST split here rather than in the tool body | extract `_split_combined_latlon_arg(tool_name, raw_args)`; the ordering constraint becomes the reason the call sits before the loop | extract |
| trid3nt_server/tools/tool_arg_normalizer.py | 851 | `normalize_args`: the `# Step 1:` .. `# Step 4b:` comments numbering the pipeline inside one function body | extract each step as a named function over `(key, value, accepted, aliases)`; the numbering disappears into a readable sequence of calls | restructure |
| trid3nt_server/tools/__init__.py | 71 | `register_tool`: "a kwarg left `None` keeps what the metadata already declares", above six identical `if x is not None` lines | extract `_metadata_overrides(**kwargs)`; the rule is then the extracted function's whole job | extract |
| trid3nt_server/tools/search/search_tools/search_tools.py | 775 | `_LEX_REINFORCE_GATE_DOOR = 3  # the wider gate` and the `tier == "door"` test it guards | no registry entry carries tier `"door"`, so the name promises a tier that does not exist; renaming to `_LEX_REINFORCE_GATE_WIDE` (or removing the dead branch) removes the need to explain it | restructure |
| trid3nt_server/tools/search/search_tools/search_tools.py | 1000 | the five-line block describing the name-substring channel inside `search_tools` | extract `_name_substring_ranking(query_tokens, index)`; `tool_retrieval._build_channel_rankings` already holds the same code, so the extraction has its second consumer waiting | extract |
| trid3nt_server/tools/search/search_tools/search_tools.py | 126 | `_DiscoverIndex`: "every per-tool list is PARALLEL to `tool_names`" | one list of per-tool records instead of eight parallel lists; the invariant becomes unstateable rather than documented | restructure |
| trid3nt_server/tools/search/search_tools/search_tools.py | 896 | the `_STOPWORDS` block on demonstratives: "the name channel STEMS a trailing 's' ... so 'this' becomes 'thi' and silently matches any tool name containing it" | extract the suffix stripper as `_desuffix(token)`, shared by both name channels; the trap becomes a test on a named function | extract |
| trid3nt_server/tools/search/tool_retrieval.py | 203 | `_full_registry_floor`: "The FULL registry is populated first, because the catalog tools register only through the startup import path" | extract `_ensure_registry_populated()`; the ordering requirement is then the call, not a note about it | extract |
| trid3nt_server/tools/search/tool_retrieval.py | 86 | "A positive allowlist of the CPU-local backends, so any FUTURE network-backed backend is excluded by default" | name the tuple `_CPU_LOCAL_DENSE_BACKENDS` and the allowlist explains itself at the membership test | rename |
| trid3nt_server/tools/search/ogc_adapter.py | 223 | `fetch_ogc_layer`: "`layer_name` is unused for ARCGIS_REST, whose layer lives in the URL" | dispatch to the per-dialect builders through a table keyed by service type, each taking only the arguments it uses; the carve-out disappears with the unused parameter | restructure |
| trid3nt_server/tools/search/ogc_adapter.py | 225 | the block on deriving a grid when both `width_px` and `height_px` are unset, and mirroring one onto the other | extract `_raster_grid(bbox, width_px, height_px, target_resolution_m)`; both rules become that function's contract | extract |
| trid3nt_server/tools/search/fetch_from_catalog/fetch_from_catalog.py | 163 | "An ImageServer endpoint does NOT support /<layer>/query at all ... The URL substring is what tells the two apart" | extract `_is_image_server(url)`; the substring test stops being a comment about a substring test | extract |
| trid3nt_server/tools/search/catalog_common.py | 46 | `_default_catalog_yaml_path`: "The catalog is curator-edited under git at the repo root; the walk normally finds it, and the trailing parents[3] is the fallback" | extract `_repo_root()`; the walk-then-fallback shape is then named once and reusable | extract |
| trid3nt_server/tools/search/search_data_catalog/search_data_catalog.py | 109 | `_bbox_overlaps_world`: the docstring has to explain that the function tests US scoping, not world coverage | rename to `_entry_may_cover_bbox`; the name currently says the opposite of the test | rename |
| trid3nt_server/tools/meta/spatial_input_tool/spatial_input_tool.py | 92 | "This body deliberately does NOT touch the websocket ... The SENTINEL carries the validated args back" | extract `_sentinel(mode, purpose, ...)`; a named constructor says the return is a sentinel without a paragraph | extract |
| trid3nt_server/tools/meta/list_run_frames/list_run_frames.py | 134 | the block explaining that the `publish_manifest` branch fires only for runs predating `outputs.json` | extract `_legacy_frames(shim, layer)`; the branch's legacy status belongs in its name | extract |
| trid3nt_server/tools/display/show_nexrad_radar/show_nexrad_radar.py | 126 | `_build_wms_url`: "It is a BASE: a renderer appends its own GetMap params to it" | rename to `_wms_base_url`; the caller then cannot mistake it for a complete GetMap request | rename |
| trid3nt_server/workflows/telemac/workflow.py | 305 | `_group`: "The dictionary's own top-level rubrique - what the advanced fold sorts by." | rename to `_rubrique`; the sort key stops needing prose at three call sites | rename |
| trid3nt_server/workflows/telemac/workflow.py | 66 | `Door.mesh_on`: "the name one of them was ``.named()`` by" | rename the field to `mesh_on_step`, and the comment is the type | rename |
| trid3nt_server/workflows/telemac/authoring/assembler.py | 289 | `_face_section`: the comment block saying an unpainted node drops out and takes no offset with it | extract `_painted_pairs(offsets, bed, nodes)`; the drop becomes a named filter | extract |
| trid3nt_server/workflows/telemac/authoring/assembler.py | 431 | `_settle_release`: two comment blocks, one per branch, explaining the derived walk and the wet-node landing | extract `_derived_release(...)` and `_wet_landing(...)`; the two blocks become the two names | extract |
| trid3nt_server/workflows/telemac/authoring/assembler.py | 585 | `settle_reach`: the comment block reconciling the measured minimum edge against the asked resolution | extract `_granularity(mesh, asked_m)` returning the size and its label together | extract |
| trid3nt_server/workflows/telemac/authoring/assembler.py | 604 | `settle_reach`: the WHAT-THE-RUN-STARTS-FROM comment block above the `initial_state` dict | extract `_initial_state(continue_from, node_xy)`; the fresh and continued cases become one named seam | extract |
| trid3nt_server/workflows/telemac/helpers/dredging.py | 96 | `_dredge_field`: "The inward offset is the BANK SETBACK, so the dig stops short of the toe" | rename `offset_m` to `bank_setback_m`; the comment is then the parameter | rename |
| trid3nt_server/workflows/telemac/helpers/dredging.py | 113 | `_at_station`: "A box wide enough for any channel crosses a MEANDER, so THIS station wins." | rename to `_piece_nearest_station`; the meander rule reads off the name | rename |
| trid3nt_server/workflows/telemac/helpers/reach.py | 351 | `measure_water_coverage`: the comment block distinguishing the terminal zero from a journalled fraction | extract `_refuse_unmapped(fraction)`; the two outcomes become two named paths | extract |
| trid3nt_server/workflows/telemac/helpers/reach.py | 176 | `_locality_tail`: "The locality tail of a compound place name - what follows near/at/by/in." | rename to `_place_after_preposition`; the docstring is then the name | rename |
| trid3nt_server/workflows/telemac/helpers/uniform_flow.py | 60 | `_wetted`: the comment block on the vertical end walls above the panel loop | extract `_panel(d1, d2, run)` returning area and perimeter; the wall rule lands on its own branch | extract |
| trid3nt_server/workflows/telemac/helpers/release_point.py | 121 | `snap_release_to_wetted`: the exempted docstring's node-and-wetness contract | extract `_nearest_wet_node(xy, mask, here)`; the docstring then states only the move | extract |
| trid3nt_server/workflows/telemac/modules/module.py | 105 | `Slot.is_open`: "Lists included: an undefaulted list is empty until something states it." | rename to `has_no_engine_default`; `is_open` is the only reason the list clause is needed | rename |
| trid3nt_server/workflows/telemac/modules/module.py | 313 | `_is_method`: "A keyword's value is data; no dictionary spells a keyword as a callable." | rename to `_is_body_code`; the data-versus-code split is then the name | rename |
| trid3nt_server/workflows/telemac/modules/sheet.py | 197 | `_late`: "Walked rather than tested: a placeholder refuses its own truth value." | rename to `_holds_unbound_read`; the walk is an implementation detail the name would absorb | rename |
| trid3nt_server/workflows/telemac/modules/sheet.py | 159 | `_standing`: "A body's assertions beat its parts; a composite or a read is PENDING." | rename to `_layers_before_fill`; the precedence reads off the name at its one call site | rename |
| trid3nt_server/workflows/telemac/modules/telemac2d.py | 116 | `_series`: "A continued run carries the SAME scenario forward, never a re-release." | rename `until_s` to `scenario_horizon_s`; the absolute-clock rule is then in the argument | rename |
| trid3nt_server/workflows/telemac/products/postprocess_telemac.py | 168 | `_pick_named_var`: "Falls back to an EXACT mnemonic match, else ``None``; it never guesses." | rename to `_first_var_matching`; the never-guess clause is the absence of a fallback, not prose | rename |
| trid3nt_server/workflows/telemac/products/postprocess_telemac.py | 851 | `postprocess_telemac_wse`: the three-paragraph comment block on the wet mask and the `quantity="depth"` exception | extract `_wse_mask(...)` and `_depth_field(...)`; the exception becomes a second named path | extract |
| trid3nt_server/workflows/telemac/products/run_reads.py | 256 | `outlet_hydrograph`: the exempted docstring's outflow-positive sign convention | extract `_outflow_positive(series)`; the negation gets a name and the paragraph goes | extract |
| trid3nt_server/workflows/telemac/products/products.py | 384 | `_publish_transported_field`: "``product`` is the reader's own and nothing here chooses between them." | rename `product` to `field_product` (or type it), so the row stops needing the disclaimer | rename |
| trid3nt_server/workflows/telemac/solving/solve.py | 254 | `_ALLOWED_COMPUTE`: "The compute ladder the dispatcher knows." | the ladder belongs to the dispatcher that serves it; importing it there removes both the constant and its comment | restructure |
| trid3nt_server/workflows/telemac/templates/shared/river.py | 62 | `PARAMS` and `RELEASE`: both docstrings exist to say which rows are the PART's rather than a question's | rename to `SHARED_PARAMS` and `RELEASE_PARAMS`; the ownership is then in the names | rename |
| trid3nt_server/workflows/telemac/templates/rain_on_grid/cn_infiltration.py | 172 | `huang_steep_slope_cn` and `paper_exponential_steep_slope_cn`: a comment block on each saying which one the engine implements | rename the rational form to `engine_steep_slope_cn`; the pair then distinguishes itself | rename |
| trid3nt_server/tools/processing/_hydrology_common.py | 289 | `# Snapping the outlet to the max-accumulation cell of the window is what guarantees it sits on the main channel rather than on a hillslope cell.` | extract `_snap_outlet_to_max_accumulation(acc, row, col, radius)`; the window search stops needing prose to say what it is for | extract |
| trid3nt_server/tools/processing/compute_slope/compute_slope.py | 218 | `# Only the last path component (the hash) goes into the id, to keep it short.` | extract the shared `_dem_key(dem_uri)` the three gdaldem tools each spell inline | extract |
| trid3nt_server/tools/processing/compute_aspect/compute_aspect.py | 217 | the same id-derivation comment, verbatim | the same shared `_dem_key(dem_uri)` | extract |
| trid3nt_server/tools/processing/clip_raster_to_polygon/clip_raster_to_polygon.py | 72 | `# boto3 owns the credential chain here, not GDAL's /vsis3/.` | one shared `stage_raster_bytes(uri)` seam; the note is repeated in four modules because the staging is | extract |
| trid3nt_server/tools/processing/compute_blended_composite/compute_blended_composite.py | 154 | `# Valid-mask precedence: an explicit alpha band, then a palette alpha, then the dataset mask, then the nodata value, else all-valid.` | extract `_valid_mask(src, rgb, palette_alpha)`; the precedence becomes the function's own ordered body | extract |
| trid3nt_server/tools/processing/compute_hillshade/compute_hillshade.py | 275 | `# 315 deg is the classic NW sun position.` and its complement note two calls below | two named constants (`_SWISS_PRIMARY_AZIMUTH`, `_SWISS_COMPLEMENT_AZIMUTH`) carry both | rename |
| trid3nt_server/tools/processing/compute_contours/compute_contours.py | 118 | `# A tie takes the smaller, denser interval.` | name the sort key (`_denser_on_tie`) instead of commenting the lambda | extract |
| trid3nt_server/tools/processing/compute_building_density/compute_building_density.py | 320 | `# A tiny or degenerate polygon still contributes one centroid rather than vanishing from the count.` | extract `_vertex_mean(pts)`; both fallbacks then call one named thing | extract |
| trid3nt_server/tools/processing/compute_impervious_surface/compute_impervious_surface.py | 389 | `# The URI token is the strongest product signal, so it is resolved here and passed down as an override of the tag inspection.` | extract `_product_override_from_uri(uri)`; the override's provenance reads off the name | extract |
| trid3nt_server/tools/processing/enhance_satellite_image/enhance_satellite_image.py | 161 | `# A leading zero makes each window sum a simple difference.` | name the array `csum_with_leading_zero` | rename |
| trid3nt_server/tools/processing/extract_landcover_class/extract_landcover_class.py | 371 | `# Validation runs before the cache lookup, so a bad call fails on its own terms rather than on a key miss.` | extract `_validated_inputs(classes, bbox)` and call it before `read_through`; the ordering stops needing a note | restructure |
| trid3nt_server/tools/processing/spatial_query/spatial_query.py | 445 | `# This COPY is composed by the tool, never by the model: the read-only allowlist over the user statement is untouched.` | the function is already `_copy_via_duckdb_gdal`; a `_tool_composed_` prefix on both export helpers carries the distinction | rename |
| trid3nt_server/tools/processing/compute_layer_bounds/compute_layer_bounds.py | 266 | the five-line comment explaining why `pad_m` is a distance and how the degrees are walked geodesically | extract `_geodesic_pad_degrees(bbox, pad_m)`; the walk names itself and only the distance-vs-fraction sentence remains | extract |
| trid3nt_server/tools/processing/compute_cross_section/compute_cross_section.py | 275 | `# src.sample takes coordinates in the raster's own CRS.` | extract `_stations_in_raster_crs(stations, raster_crs)`; the same note sits in `compute_flood_depth_damage` at line 402 | extract |
| trid3nt_server/tools/processing/compute_change_detection/compute_change_detection.py | 451 | `# Web Mercator area is adequate for a speck filter at AOI scale.` | name the column `area_m2_webmercator` so the approximation travels with the value | rename |
| trid3nt_server/tools/processing/compute_ndvi/compute_ndvi.py | 166 | `# Between the comfort window and the cap nothing raises: the px-clamp already coarsens the grid, so the note is what makes the resolution trade visible rather than silent.` | extract `_note_auto_coarsen(area, notes)`; the branch's whole job is the note | extract |
| trid3nt_server/tools/processing/compute_flood_depth_damage/compute_flood_depth_damage.py | 271 | `# A non-point geometry degrades to its centroid at screening resolution.` | extract `_points_only(gdf, notes)` | extract |
| trid3nt_server/tools/processing/extract_model_at_observations/extract_model_at_observations.py | 774 | the four-line comment enumerating the three quantity-cross outcomes | extract `_reconcile_quantity(...)`; the enumeration becomes its branches | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 240 | `# Case-insensitive enum: normalize BEFORE the allowed-set check` | extract `_coerce_enum(pspec, value)` out of the `validate_params` branch chain | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 271 | `# A 2-element [start, end] int list. Element bounds: min floors start, max ceils end` | extract `_coerce_int_range` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 291 | `# A 2-element [start, end] ISO datetime-pair.` | extract `_coerce_datetime_range` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 313 | `# A 2-element [lon, lat] float list. Coerce + finite/range check` | extract `_coerce_lonlat` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 331 | `# A scalar float OR a list[float]. Each entry must be in the allowed values set` | extract `_coerce_float_or_list` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 358 | `# A scalar string OR a list[str] free-text filter` | extract `_coerce_str_or_list` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 374 | `# A truthy flag. Coerced with bool(value)` | extract `_coerce_flag` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 380 | `# Accept 'YYYY-MM-DD' or 'YYYYMMDD'; normalize to the 8-digit compact form` | extract `_coerce_date_compact` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 396 | `# Alias-or-passthrough: lower/strip -> table, else verbatim` | extract `_coerce_aliased_str` | extract |
| trid3nt_server/tools/fetchers/_router/router.py | 525 | `# Record-return path: a shape: record source produces a bare JSON dict` | `select_executor` is a fourteen-branch ladder whose every branch is announced by a comment; a `(predicate, executor)` table named per row removes all of them | restructure |
| trid3nt_server/tools/fetchers/_router/router.py | 531 | `# Sidecar-write path: a source whose read ALSO yields ONE declared sidecar object` | the same executor-selection table | restructure |
| trid3nt_server/tools/fetchers/_router/router.py | 557 | `# Chained-resolution path: a spec that declares an offset-paging hook` | the same executor-selection table | restructure |
| trid3nt_server/tools/fetchers/_router/router.py | 566 | `# Tier-3 hook-driven path: a spec that names a build_request hook` | the same executor-selection table | restructure |
| trid3nt_server/tools/fetchers/_router/router.py | 879 | `# Record-return path: a shape: record source caches its JSON dict bytes` | extract `_route_record(spec, params, metadata, executor)` out of `_route_once` | extract |
| trid3nt_server/tools/fetchers/_router/shape_classifier.py | 109 | `# 1a. ArcGIS/ESRI error envelope: {"error": ...} inside an otherwise-valid JSON object` | extract `_arcgis_envelope(body)` returning a verdict or None | extract |
| trid3nt_server/tools/fetchers/_router/shape_classifier.py | 129 | `# 1b/1c. XML-shaped error envelopes` | extract `_xml_envelope(text)` returning a verdict or None | extract |
| trid3nt_server/tools/fetchers/_router/shape_classifier.py | 147 | `# 2. A parsed JSON value that isn't a recognized error envelope.` | with the two envelope extractions, the numbered running order reads off the returns | extract |
| trid3nt_server/tools/fetchers/_router/executors/dataretrieval_delegate.py | 180 | `# 1. Station service -- the authoritative monitoring-location locations.` | extract `_wqp_stations`, `_wqp_latest_results` and `_wqp_join` out of `wqp_features` | extract |
| trid3nt_server/tools/fetchers/_router/executors/dataretrieval_delegate.py | 209 | `# 2. Honest typed error if no sites -- never an empty success layer.` | the same extraction: the raise belongs inside `_wqp_stations` | extract |
| trid3nt_server/tools/fetchers/_router/executors/dataretrieval_delegate.py | 229 | `# 4. Left-join: one record per station; latest result decorates (or nulls).` | the same extraction | extract |
| trid3nt_server/tools/fetchers/_router/hooks/topobathy.py | 1236 | `# 1) CUDEM tiles, best-effort: empty means no coverage.` | `_select_and_merge` runs seven numbered legs in one body; extract one function per leg | extract |
| trid3nt_server/tools/fetchers/_router/hooks/topobathy.py | 1327 | `# 4) Merge / reproject -> composite array.` | the same leg extraction | extract |
| trid3nt_server/tools/fetchers/_router/hooks/topobathy.py | 1387 | `# 4b) PROMISE vs PAINT. The pre-fetch coverage gate reads tile FOOTPRINTS` | extract `_reconcile_promise_against_paint(...)`; the name states the rule the comment states | extract |
| trid3nt_server/tools/fetchers/hydrology/fetch_noaa_nwm_streamflow/hooks.py | 468 | `# 1. Resolve + download the NWM netCDF.` | `_fetch_nwm_features` runs four numbered rounds in one body; extract one function per round | extract |
| trid3nt_server/tools/fetchers/socioeconomic/geocode_location/geocode_location.py | 128 | `# (1) whole normalized query as a full state name.` | `_extract_us_state` is a four-step ladder; extract `_match_full_name`, `_match_stripped`, `_match_tail` and `_match_abbreviation`, each returning a state or None | extract |
| trid3nt_server/tools/fetchers/socioeconomic/geocode_location/geocode_location.py | 159 | `# (3) explicit 2-letter USPS abbreviation with word-boundary guards.` | the same ladder extraction | extract |
| trid3nt_server/tools/fetchers/socioeconomic/geocode_location/geocode_location.py | 185 | `# (4) last resort: hand the whole stripped string to the shared resolver` | the same ladder extraction | extract |
| trid3nt_server/tools/fetchers/us_states.py | 83 | `# 2-letter code fast path.` | `resolve_state_code` is two paths in one body; extract `_by_code` and `_by_name` | extract |
| trid3nt_server/tools/fetchers/us_states.py | 84 | `# Full-name path: strip noise prefix, collapse whitespace, lowercase.` | the same extraction | extract |
| trid3nt_server/tools/fetchers/_fetch_common.py | 99 | `# 1 degree of latitude ~ 111_320 m; 1 degree of longitude ~ 111_320 * cos(lat) m.` | name the literal `_M_PER_DEG_LAT = 111_320.0`, used three times below | rename |
| trid3nt_server/tools/fetchers/_router/executors/vector_fgb.py | 323 | `# Column-map projection runs FIRST so geometry_filter / json_coerce / derived_columns see the normalized output props` | the ordering is the fact: a named pipeline tuple of the four stages puts it in the data | restructure |
| trid3nt_server/tools/fetchers/_router/executors/station_timeseries.py | 388 | `# observed: pick the most-recent valid (speed, direction) row.` | extract `_latest_observed(rows)` beside the existing nearest-prediction path | extract |
| trid3nt_server/tools/fetchers/imagery/_goes_archive_core.py | 686 | `# The ramp runs orange at the low end to yellow at the middle to white at the top` | the ramp is a table: `_HOT_RAMP = ((0.0, ORANGE), (0.5, YELLOW), (1.0, WHITE))` interpolated by a named helper | restructure |
| trid3nt_server/tools/fetchers/_router/executors/raster_cog.py | 1782 | `# The ImageServer exportImage response IS the artifact: no reserialize.` | `execute` branches per access mode with a comment each; a `{mode: builder}` table named per row removes them | restructure |
| trid3nt_server/tools/fetchers/_router/hooks/topobathy_class.py | 165 | `# Two verdicts are decidable from that evidence and one is not` | extract `_tidal(features)` and `_no_mapped_water(features)` predicates; the verdicts then read off their names | extract |
