# Repo metrics ledger

Progress tracking toward web-orphan removal + modular architecture (NATE
2026-07-27). Append a row per milestone; regenerate counts with the command
below. LOC = python lines incl. comments/blank (consistent measure - trend
matters, not the absolute).

    for d in server/src/trid3nt_server server/tests contracts/src \
      contracts/tests services/workers qgis-plugin/trid3nt scripts; do \
      find $d -name "*.py" -not -path "*__pycache__*" | xargs wc -l \
      | tail -1; done

| date | server pkg | server tests | contracts | workers | plugin | scripts | registry | server.py | notes |
|---|---|---|---|---|---|---|---|---|---|
| 2026-07-27 | 224,142 (563f) | 179,409 (423f) | 11,675+9,483 | 47,897 | 14,300 | 6,255 | 211 | 15,441 | post engine-door rollout, pre cull-pass-2 landing; server.py monolith flagged (cards extraction queued); Mexico Beach scripts + hygiene sweep pending |
| 2026-07-27b | 219,681 (552f) | 176,598 (414f) | - | - | - | - | 202 | 15,441 | post cull (9 tools, replication-proven) + structural batch (agent/ umbrella, search/, 35 dead files); -4,461 pkg lines, -11 files, -9 registered tools vs morning row; suite true baseline = 10 |
| 2026-07-28 | 219757 (558f) | - | - | - | - | - | 200 | 13,910 | post hygiene sweep: server.py -1,529 (cards extracted), comment archaeology -1,785 across 264 files (comment lines 25,317 -> 25301), meta renames, ADR offload notes; suite baseline 10 |
| 2026-07-28b | 218614 | - | - | - | - | - | 200 | 13,228 | recall pass: narrative blocks 216->16, war-stories 0, -1143 lines, comment-lines -> 24502; 9 genuine-architecture ADRs |
| 2026-07-28c | 217252 | - | 11381 | - | - | - | 200 | 13,072 | dynamic hot-set + mongo_collections cut: Mongo stratum fully closed; lessons/vertex-log/deadname-default/plugin-button batch landed

## Folder-level view (added 2026-07-28, regenerate per milestone)

    cd server/src/trid3nt_server && for d in */ agent/*/; do \
      find "$d" -name "*.py" -not -path "*__pycache__*" | xargs wc -l | tail -1; done

| date | agent/tools | agent/workflows | agent/other | AGENT total | root files | emission | sandbox | credentials | PLATFORM total |
|---|---|---|---|---|---|---|---|---|---|
| 2026-07-28 | 119,548 | 57,056 | 12,406 | 189,010 (87%) | 20,785 | 4,190 | 2,081 | 1,665 | 28,721 (13%) |
| 2026-07-28d | 216078 | - | - | 32878 | - | - | 200 | 13,116 | SFINCS remediation: flood.py 5637->2405 (conformance: run_sfincs + forcing_autowire), deckbuilder worker gone, quadtree/waves arm deleted, shared hoist; TRUE BASELINE NOW 9 (coastal fixed) | 
| 2026-07-29 | 214738 | - | - | - | - | - | 200 | 13,116 | fetcher-fold wave 1: 5 twins deleted (-5,735), spec-served surfaces live, experiment SUPPORTED byte-identical |
| 2026-07-29b | 211629 | - | - | - | - | - | 200 | 13,116 | fetcher-fold wave 2 (ArcGIS vector family): 6 twins deleted (net -5,306 incl. tests), router gains declarative where/column_map/fallback-chain/endpoint_select; 3 deferred by stop-rule (ejscreen, slr, levees) |
| 2026-07-29c | ~210060 | - | - | - | - | - | 200 | 13,116 | fetcher-fold wave 3 (USGS via dataretrieval): wqp+nldi folded (-1,570 py), gw-levels+nwis-gauges deferred by stop-rule |
| 2026-07-29d | ~206500 | - | - | - | - | - | 198 | 13,116 | cull phase A: news+conservation+goes wrappers out (live-gated), fetch_slider_timestamps in; satellite deferred on FIRMS key |
| 2026-07-29e | ~206850 | - | - | - | - | - | 197 | 13,116 | cull phase B: glm wrapper out (P2 dropped, moving-base proven live); shared/ model_* residuals now 1 (satellite, FIRMS-key gated) |
| 2026-07-29f | ~204600 | - | - | - | - | - | 191 | 13,116 | processing wave: generate_chart generic primitive in, 4 fixed-shape charts + clip_bbox + zonal + aggregate tools out (all live-gated); folder at redundancy floor |
| 2026-07-29g | ~202700 | - | - | - | - | - | 191 | 13,116 | fold wave 4 (stations): coops_currents folded (net -1,590), 5 deferred by stop-rule; twin-comparison experiment machinery retired |
| 2026-07-29h | ~204000 | - | - | - | - | - | 191 | 13,116 | ingest transport (ADR 0044): httpx opener owns remote-file sockets, direct_window off vsicurl (parity sha-identical), 404->EMPTY restored; STAC-tile seam residual |
| 2026-07-29i | 204730 | - | - | - | - | - | 190 | 13,116 | satellite preemptive cull (NATE waiver, ADR 0046): last shared/ model_* wrapper out; corpus re-homed w/ retrieval proof |
| 2026-07-29j | ~204790 | - | - | - | - | - | 190 | 13,116 | fold wave 5 (raster): STAC-tile seam onto transport (byte-identical live); 11 raster twins deferred by stop-rule, 6 mode-enablers scoped in ADR 0047 |
| 2026-07-29k | ~204510 | - | - | - | - | - | 190 | 13,116 | processing de-cloud (ADR 0048): _gdal_runner shared module, in-process COG encoder, grace2 fallback deleted, golden parity 4/4; flood canary PASSED (depth COG + overviews via in-process encoder) |

## CANONICAL METRIC (NATE 2026-07-30): CODED tools, rolling deltas

The registry total counts REGISTRATIONS incl. spec-synthesized data-tools
and is NOT the reduction metric. The metric is HAND-ROLLED PYTHON TOOLS
(coded = registered minus _router._promoted) and their LOC, reported as
rolling n-1 -> n deltas per landing, never arc-start comparisons.

| date | coded tools | coded fetchers | spec-served (data) | bespoke fetcher LOC | debug surface note |
|---|---|---|---|---|---|
| 2026-08-04o | 87 (+3) | 2 (+0) | 95 (+0) | ~34,860 (+0) | HAZARD EASY-FOUR CONTINUATION LANDED (ADR 0123): rows 2-4 of ADR 0122, all three landed with LIVE cheap-smoke evidence. +3 NEW capability-named CODED templates (hand-written `@register_tool`, tier=template): coded tools 84->87, registered 182->185 (test_catalog_surfacing pins bumped x4 + header), templates 24->27, category `hazard_modeling`. Coded fetchers / spec-served UNCHANGED (no data source touched). (1) `landlab_green_ampt_overland_flow` (engine=landlab, EXEC-mode, no rebuild): new `analysis="green_ampt_overland_flow"` branch in `component_chain.py` -- SoilInfiltrationGreenAmpt + de Almeida OverlandFlow storm PARTITION (dt-capped >=40 substeps); PRIMARY infiltration-depth raster + SECONDARY runoff-depth (rainfall-excess) raster + partition chart; triggering rainfall from the real NOAA Atlas-14 design storm (0102 seam), soil block demo-labeled synthetic_inputs; new contract `LandlabGreenAmptLayerURI`. LIVE exec smoke (run_chain.py, synthetic UTM DEM, seconds, no MinIO): infiltrated_fraction 0.79 / runoff_fraction 0.21 / mean_infiltration 35.5 mm; K-monotonicity + determinism verified. (2) `elmfire_verification_elliptical_replication` (engine=elmfire, DOCKER): ALL-CONSTANT flat-grid deck authored AGENT-SIDE (`build_constant_verification_deck` + `deck_builder.write_constant_raster_typed`; GR2/102 fuel + flat terrain + uniform wind, NO LANDFIRE/DEM fetch), ToA perimeter vs the Richards ellipse (`verify_elliptical_replication`) -> verification triple (rmse/err/corr-class) + ellipse-overlay chart; new contract `ElmfireEllipseVerificationLayerURI`. LIVE docker smoke (`trid3nt/elmfire:dev` bash-compatible image -- the trid3nt-verify image has a python entrypoint incompatible with the local-docker `bash -c` spec, a documented substrate fact; 10 km domain, 15 mph, 1.5 h): err_fraction 0.0375 (< 0.08 COARSE tolerance) / correlation 0.986 (good) / LW 3.41 / passed=True; real ToA s3 URI. NAMED RESIDUAL: the fine-grid <0.5% published Verification-01 gate (Rothermel-rate cross-check). (3) `geoclaw_tsunami_gauge_timeseries` (engine=geoclaw, DOCKER): widened the composer's `fort.*`-only download filter to include `gaugeNNNNN.txt` (`_is_geoclaw_output_key`); gauge parser + surface-elevation time-series chart (`parse_geoclaw_gauge_series` / `build_gauge_timeseries_chart_spec`); OPTIONAL gauge scalars added to `GeoClawDepthLayerURI` (additive, default None); `model_geoclaw_inundation` gains `emit_gauge_series`; the template rides it (scenario forced tsunami). LIVE docker smoke (Crescent City AOI, amr=2, 600 s): real tsunami solve -> gauge file downloaded (filter widening works) -> parsed -> gauge_max_surface_elevation 3.64 m / amplitude 0.43 m / coseismic_offset 3.64 m + real ToA s3 URI (gauge on high dry ground in the cheap config -- pipeline proven). Model-free retrieval (retrieve_visible_tools, None, 8): all three top-8 for their 5 phrasings. Offline tests: server test_landlab_green_ampt (8) + test_elmfire_elliptical_verification (7) + test_geoclaw_gauge_timeseries (6) + 2 worker green_ampt (real-landlab-gated). Hygiene lint PASSES (all 27 templates). Board rows infiltration_coupled_runoff_generation + constant_wind_elliptical + elliptical_exact_solution + geoclaw gauges Today flipped CAND/section->LANDED. No flood seam touched (grep-verified; the geoclaw change is additive gauge parsing, not a flood physics seam; no canary mandated). |
| 2026-08-04n | 84 (+1) | 2 (+0) | 95 (+0) | ~34,860 (+0) | HAZARD EASY-FOUR WAVE #1 LANDED (ADR 0122): `landlab_flow_accumulation` -- a NEW capability-named CODED template (hand-written `@register_tool`, engine=landlab tier=template), NOT an enum extension of `landlab_susceptibility` (flow accumulation is a distinct question class per the capability-naming rule; the registration cost is +1 tool honestly). Coded tools 83->84, registered 181->182 (test_catalog_surfacing pins bumped x4 + header), templates 23->24, category `hazard_modeling`. Coded fetchers / spec-served UNCHANGED (no data-source touched; the DEM is the existing fetch_3dep_extra->fetch_dem seam). Exec-mode (no image rebuild): new `analysis="flow_accumulation"` branch in `component_chain.py` -- FlowAccumulator (or PriorityFloodFlowRouter) with flow_director D8/Dinf/MFD (via advanced_physics/physics_registry) + depression_handler (fill=DepressionFinderAndRouter D8-only | priority_flood=PriorityFloodFlowRouter any director, THE folded ADR 0121 row 9) + channel_threshold_cells knobs; PRIMARY output = drainage_area (m^2, `continuous_drainage_area` style -- a dedicated log-DOMAIN TiTiler expression is a NAMED RESIDUAL); SECONDARY = channel_network mask + slope. Postprocess `postprocess_landlab_flow_accumulation` reprojects the drainage COG to 4326, vectorizes the channel mask -> EPSG:4326 GeoJSON channel-network vector, and builds the D8/Dinf/MFD routing-comparison Vega-Lite chart (the the_FlowAccumulator tutorial's central figure). New contract `LandlabFlowAccumulationLayerURI` (max/mean_drainage_area_km2 + channelized_area_fraction) + `LandlabDepressionHandler` literal. LIVE exec smoke (run_chain.py, synthetic UTM DEM, seconds, no MinIO): drainage COG + channel + slope COGs + result.json flow_accumulation block; max drainage_area 4.176 km2, channelized fraction 0.011, routing_comparison 3 rows (D8 0.011 / Dinf 0.129 / MFD 0.123); determinism verified (identical field on re-run). Retrieval corpus-first: `landlab_flow_accumulation` in top-8 for all 4 flow-accumulation phrasings (model-free retrieve_visible_tools). Offline tests: 2 worker (determinism + 3-director comparison, real landlab) + 9 server (contract/build_spec/chart/vectorization-fixture/gate). Hygiene lint PASSES. NEW LOC ~+1,460 (composer 541 + postprocess +~200 + worker +~180 + contracts +~70 + server test 194 + worker test 59 + corpus). Board rows flow_accumulation_standalone + multi_flow_direction + priority_flood_large_aoi all flipped CAND->LANDED (folded into the one template). No flood seam touched (grep-verified; no canary mandated). |
| 2026-08-04m2 | 83 (+0) | 2 (+0) | 95 (+0) | ~34,860 (+0) | S-TIER TEMPLATE WAVE 2 (ADR 0121): hazard cluster (openquake/pelicun/geoclaw/landlab/elmfire/swan/swmm), 13 candidate rows. HONEST TRIAGE, 0 LANDED -- ground-truth (worker/deck/library code read directly) showed NONE is a pure-knob drop-in over an already-plumbed product surface with a byte-identical baseline (the bar the one wave-1 landing met); the [S] labels were optimistic across the board, a SECOND confirmation of the wave-1 lesson. No coded-tool / registry / template / category / retrieval / schema change => offline baseline undisturbed (documented exactly-9-failure set stands by construction). LOAD-BEARING SUBSTRATE FACT: exec-mode/library engines (openquake/swmm/landlab pip via exec_kind=exec; pelicun in-process) run worker code from the LOCAL checkout (no image rebuild; landlab/pyswmm/openquake/pelicun confirmed in venvs/agent, clawpack/geoclaw NOT); docker engines author decks either INSIDE the image (geoclaw setrun_builder, swan deck_builder -- baked, rebuild to change) or ON THE AGENT HOST (elmfire deck_builder -- live drop-in). VERDICTS: STOP-M/L needing new worker/library deck-authoring = 1 openquake_scenario_shaking (calc_mode hardwired classical, zero rupture_model.xml authoring, no GMF harvest), 2 openquake_logic_tree (single-branch renderers, gmpe=single str, quantiles= hardcoded empty, no quantile-curve harvest), 5/6/7 pelicun (the tool never calls pelicun DL_calculation/Assessment at all -- bespoke numpy over ONE bundled HAZUS-flood csv; FEMA-P58/HAZUS-EQ/aggregation all need the real assessment engine; fema_hazus_eq_2020 is a stubbed-to-raise enum), 12 swan HSWELL (deck_builder baked, HSWELL not in _VALID_OUTPUT_QUANTITIES, no QUANTITY render path -> rebuild), 13 swan regression (entrypoint always re-renders from narrow SwanBuildSpec, no raw-deck passthrough). ALREADY-COVERED = 3 swmm_wq_buildup_washoff (buildup/washoff pollutograph + load + peak-conc layer already ship in swmm_urban_flood(pollutants=...) #224; a dedicated single-subcatchment deck is a duplicative M fixture, not recommended). STOP-but-genuine-server-side/exec-mode FEATURE builds (NO image rebuild; image/worker already emits the raw output; recommended next-build subset, cheapest-first) = 8+9 landlab_flow_accumulation (fold priorityflood into flow-accum; FlowAccumulator+flow_director D8/Dinf/MFD+DepressionFinderAndRouter already wired, drainage_area already a secondary field, PriorityFloodFlowRouter in venv; needs a new analysis branch + depression_handler knob + style/metrics; exec-mode smoke = direct run_chain.py subprocess, seconds, no MinIO), 10 landlab_green_ampt (SoilInfiltrationGreenAmpt in venv; adds infiltration-vs-runoff to the overland chain), 11 elmfire_verification_elliptical (strongest calibration anchor, closed-form ellipse <0.5% tol; deck authored agent-side, ToA raster already emitted; needs fuel_model constant-raster override + isochrone-vectorize/ellipse-diff postprocess + chart), 4 geoclaw_tsunami_gauge_timeseries (gauge already authored + gaugeNNNNN.txt already uploaded; needs widening the _download_batch_geoclaw_outputs fort.-only filter + a gauge parser/chart). Each is a contained feature build handed forward as its own scoped job rather than forced half-verified this session (close-out + live-E2E rules). Flood seams untouched (no canary). |  flood/hydraulics cluster. +1 CODED template tool (`sfincs_advanced_numerical_physics_knobs`, hand-written `@register_tool`): a strict opt-in over the already-plumbed SFINCS advanced-physics deck surface (theta/alpha/advection/huthresh/wind_drag/coriolis_latitude validated + range-checked through the shared `physics_registry`, forwarded to the SFINCS flood composer; no-knob run byte-identical to baseline; solver-stability/runtime sensitivity surface, NOT a calibration surface, so no chart -- deliverable is the depth map + the labelled settings delta; viscosity/friction2d a named residual). RENAME `hecras_muncie_flood` -> `hecras_riverine_flood` (registry tool name + `HECRAS_SOLVER_NAME` solve-seam key + folder/file + docstring + corpus + all consumers/tests; worker archetype `muncie_riverine_flood` + baked deck filenames UNCHANGED = image contract; old name gone from the index; new name + legacy "Muncie" phrasing both retrieve top-8). NEW template-hygiene GATE (`server/tests/test_template_hygiene.py`): mechanical lint over every tier=template tool doc + module doc + comments (banned: north-star / formerly / renamed-from / folded-from / `model_*_scenario` / non-ASCII); 16 existing template modules cleaned to pass (non-ASCII arrows/dashes/math-symbols -> ASCII, scenario-era prose names -> "the composer", one folded-from parenthetical dropped), zero suppressed. Registry 180 -> 181 (authoritative full-registration) / templates 22 -> 23. NEW LOC ~+470 (numerical_physics template ~285 + test_template_hygiene ~95 + test_sfincs_numerical_physics ~90; fixer edits net-neutral). HONEST STOP on 6 candidate rows the [S] label over-scoped (worker-side deck/archetype wiring + image rebuilds, not knobs): sfincs_structures_weirs_thindams + sfincs_observation_points_lines (`setup_structures`/`setup_observation` not wired), sfincs_advanced_infiltration_methods (CN-only today; Green-Ampt/Horton need per-method raster ingestion), hecras_1d_steady_bridge (RasSteady has no worker archetype + no baked bridge deck + 2.2GB rebuild), telemac2d_weir/bridge_hydraulics (new .cas/.cli/geometry recipes). LIVING ATLAS WAVE (ADR 0117): the public ESRI Living Atlas becomes a discoverable two-pool data population. +2 CODED tools (both hand-written `@register_tool`): `search_living_atlas` (BM25+dense over the harvested entries, NATE's authoritative-default / community-opt-in-or-last-resort composition) + `fetch_living_atlas_layer` (a GENERIC dynamic-SourceSpec bridge that rides `route()` -- Image->imageserver_export, Map->mapserver_export, Feature->esri_json). coded fetchers +0 and spec-served +0: the fetch bridge is a generic runtime-spec router client, NOT a bespoke per-source coded fetcher, so it does NOT count against the fold-to-zero endgame (no `source.yaml`, no twin). Registry 173->175 in-process / 177->179 daemon (test_catalog_surfacing pins 177->179 x4 + header). NEW coded LOC ~+885 across 4 server modules (living_atlas_common 190 + living_atlas_index 145 + search_living_atlas 130 + fetch_living_atlas_layer 300 -- OUTSIDE the bespoke-fetcher LOC column, which tracks the spec-fold campaign) + contract `LivingAtlasLayerURI` (~18). The two harvested catalogs (`data/living_atlas/living_atlas_authoritative.yaml` 3434 entries + `_community.yaml` 3303 entries) are DATA, excluded from coded LOC; `scripts/harvest_living_atlas.py` (224) is ops tooling. AUTHORITATIVE-FIELD VERDICT: ESRI `contentStatus` (`public_authoritative`/`org_authoritative`) -- verified live, not a `q=` filter (in-code split); premium = `typeKeywords` Requires Subscription/Credits. DYNAMIC-SPEC VERDICT: rides `route()` for free (registry-free, spec-driven), cache disambiguated by per-item `source_class=living_atlas_<id>`. LIVE PROOFS (MinIO + real ArcGIS): (1) harvest 6737 items 703 Image/5591 Feature/443 Map, 3434 auth/3303 community, 1030 premium; (2) search('wetlands') authoritative-only default (community EXCLUDED), include_community=true appends labelled community below auth; (3) US Rangelands ImageServer -> COG 10.9 MB s3; (4) Alabama Census Blocks FeatureServer -> FGB 6.2 MB s3; (5) Chlorophyll-a premium -> LIVING_ATLAS_SUBSCRIPTION_REQUIRED. Retrieval corpus-first: search_living_atlas 10/10 + fetch_living_atlas_layer 8/8 top-8. Offline suite baseline preserved 9-by-SET. Flood seams untouched (no canary). |
| 2026-08-04k | 80 (+0) | 2 (+0) | 95 (+0) | ~34,860 (+0) | REMOTE STREAMING WAVE (ADR 0116): streaming is THE path, the download/export machinery DELETED. Coded/fetcher/spec-served counts UNCHANGED (no registered tool touched; hydrate_case_layers was already deregistered, /api/export-qgis routes are not tools; registry 177 pinned by test_catalog_surfacing, unchanged). **NET -3,638 LOC** (574 added, 4,212 deleted) across plugin+server. Deleted machinery (source): `case/case_export.py` whole module (459) + server `cases/hydrate_case_layers.py` whole module (859) + `tool_catalog_http.py` export routes (~208) + `net/tasks._ExportTask` (~57) + `render/layers.materialize_export`+`_apply_named_style`+`last_added_export_extent` (~148) + `ui/dock.hydrate_case_layers`+`_on_export_*`+`_export_tasks` (~66) ~= 1,797 condemned source LOC. Deleted tests: test_export_qgis_http_route (271) + test_open_case_in_qgis (426) + test_open_case_in_qgis_mesh (416) + the export classes in plugin test_milestone2/3 + the stale ADR-0103 test_remote_mode_is_honest_skip. NEW capability (~120 LOC + test_remote_streaming.py 281): session-scoped staging TTL (`trid3nt_session_<tag>`, owner-PID-marked, cleaned on disconnect/close, dead-owner-only stale sweep at plugin start) + `_add_mesh` MDAL staging fallback + STREAMED-vs-STAGED labels. Per-format matrix verified LIVE vs the tailnet MinIO (100.92.163.46:9000): COG /vsicurl ranged (4.76 MB DEM opened reading 21% of the object; 4.5 MB at 5.3%), FGB /vsicurl spatial-index ranged, MDAL netCDF CANNOT vsicurl -> stages (the ONE fallback, labeled). 5 live proofs green (raster+FGB stream no local copy; 1.79 MB mesh stages+cleans; sweep reaps dead-owner keeps live-owner). SCHISM output contract pinned (clip-to-AOI+COG, no raw continental netCDF layer). DELETION_LEDGER rows 19/20/47 + the 0103 ws_url_to_http_base orphan flipped DELETED. |
| 2026-08-04j | 80 (+0) | 2 (+0) | 95 (+0) | ~34,860 (+0) | SCHISM SPIKE (ADR 0115): new worker `services/workers/schism/` -- NO server code, NO registered tool, coded/fetcher/spec-served counts UNCHANGED (registry byte-identical by construction; git shows only the worker dir + docs touched). WORKER LOC +591 python (schism_gr3 bridge 290 + entrypoint 108 + test 113 + qa_gate 80) + 168 Dockerfile. IMAGE `trid3nt-local/schism:latest` = **650 MB uncompressed / 155 MB compressed** (leanest solver worker: hecras 2.2 GB, telemac 3.55 GB, mesh 1.33 GB for comparison). Breakdown: postprocess venv (numpy/xarray/netCDF4/pandas) 247 MB; runtime libs (netCDF-Fortran + OpenMPI + HDF5 + libgomp) 86.6 MB; python:3.11-slim-bookworm base ~148 MB; the TWO stripped executables (full-monty 6.9 MB + hydro-core 3.6 MB) 10.5 MB; worker code 0.18 MB. Hygiene: multi-stage kept the gfortran/C toolchain + `*-dev` headers + the ~700 MB SCHISM source tree in the build stage (never in runtime). Build v5.11.0 (pinned commit 4d350e4), gfortran 12.2 + OpenMPI 4.1 + netCDF-Fortran 4.5.4. In-image QuarterAnnulus gate GREEN (analytical M2: RMSE 0.0155 m, amp err 0.0027 m, corr 0.9988). |
| 2026-08-04i | 80 (-1) | 2 (-1) | 95 (+1) | ~34,860 (twin -869; hook +~570) | FETCHER FINALE ENDGAME (ADR 0112): fetch_noaa_nwm_streamflow FOLDED -- THE LAST coded data-fetcher. coded tools 81->80, coded fetchers (package) 3->2, spec-served 94->95; **campaign coded-data-fetcher counter 1 -> 0 -- THE ARC 98 -> 0 COMPLETES.** Every data source is now spec-served through the router; the 2 coded fetchers that remain (geocode_location, lookup_precip_return_period) are NON-data-source lookups/primitives (a name->coord geocode + a scalar Atlas-14 design-storm depth), not layer-producing data ingests. Registry 177 UNCHANGED (twin died, spec took the name). DESIGN CHOICE = the whole-tool library_delegate VECTOR spec (option a, the 4th-proven precedent after topobathy/storm_tracks/goes) -- the 0069 multi-source-COMPOSITE STOP RESOLVED: the delegate nwm_streamflow.read OWNS the S3 channel_rt whole-object netCDF read -> {feature_id: streamflow} lookup + the NLDI 5x5 spatial sample (25 point-snaps) -> COMIDs + per-reach geometry (up to 500 reqs) + the in-process feature_id JOIN -> point GeoJSON features for the shared vector_fgb serializer; the join is plain in-process compute + each NLDI probe is an independent best-effort request (no transport coalescing/retry semantics needed), so the delegate hits NO wall (new machinery for one source fails the ADR 0056 bar; option b honestly considered + rejected). nwm_streamflow.validate = CONUS-intersect (twin domain -130,20,-60,55) + short_range fhour>=1 + valid_time ISO parse (pre-cache); nwm_streamflow.envelope = twin nwm-streamflow-{product}-{seed} id/name + reference-time/reach-count/NLDI-sample provenance (ADR 0110 channel). NWMStreamflowLayerURI -> contracts LAYER_RESULT_MODELS; NWMStreamflow*Error -> hooks/nwm_streamflow.py base FetchError (invoke passthrough preserves NWM_STREAMFLOW_INPUT_ERROR/_UPSTREAM_ERROR/_NOT_AVAILABLE/_EMPTY). Edge matrix EXACT (out-of-CONUS INPUT / no-reaches EMPTY / stale-object NOT_AVAILABLE / NLDI-down -> best-effort collapse to EMPTY, twin contract preserved). Docstring byte-verbatim into the spec (retrieval 8/8 corpus queries rank-1 top-8). Twin DELETED (~869 LOC); tests -> test_router_nwm_streamflow.py (24 passed, incl. the provenance cache-hit replay). CONSUMERS: sfincs_forcing_autowire re-pointed to the registry closure (import-only, grep-verified NO flood seam -- flood.py/inundation.py/wave_field.py untouched -- canary NOT mandated); telemac river_dye ADR 0102 discharge seam already seam-1, unbroken. Baseline EXACTLY 9 by SET; river_dye 5 members identical-in-kind (captured before/diffed after); catalog_surfacing spec-count pins 94->95 / -93->-94 / 93->94; daemon boot clean (registry 177). DELETION_LEDGER nwm row DELETED -- the LAST fetcher-fold row closes. |
| 2026-08-04h | 81 (-2) | 3 (-2) | 94 (+2) | ~35,360 (twins -2,105; hooks+_goes_common +1,765 incl. relocated shared substrate) | FETCHER FINALE WAVE 2 (ADR 0111): fetch_storm_tracks + fetch_goes_satellite BOTH FOLDED. coded tools 83->81, coded fetchers 5->3, spec-served 92->94; campaign coded-data-fetcher counter 3->1 (only nwm remains). Registry 173 UNCHANGED (2 twins died, 2 specs took the names). STORM_TRACKS: the ADR 0090 decisive blocker (active-mode's SECOND round = a BINARY zip-shapefile) is expressed as the SANCTIONED delegate socket impurity, NOT a new executor phase (topobathy precedent, DESIGN CHOICE option b) -- a library_delegate VECTOR spec whose storm_tracks.read OWNS both rounds (historical IBTrACS storm-wise full-track select -> line/point features; active NHC JSON -> per-storm forecastTrack.zipFile -> tempdir extractall -> geopandas.read_file -> reproject) and returns GeoJSON features for the shared vector_fgb serializer; validate = historical bbox-required + shape; resolve (pre_resolve) = storm_name canon + season-window INTO the cache key; envelope = storm-tracks-{seed} id/name + mode+attribution provenance (ADR 0110 channel). GOES_SATELLITE: the THIRD GOES access surface -- a library_delegate RASTER spec (goes_satellite.read lists the most-recent MCMIPC key, CF-scales the band, returns (array,transform,crs) for the shared DEFLATE float32 COG writer); pre_resolve rounds valid_time DOWN to 15 min + normalizes the satellite token INTO the cache key; validate = BBOX_REQUIRED + band/satellite + CONUS honest fast-reject (GOES_EMPTY); the em-dash (U+2014) LayerURI name is PRESERVED byte-identical (envelope, source stays ASCII via a unicode escape; one-line follow-up noted); scan-time provenance via the channel. Shared GOES substrate RELOCATED twin->imagery/_goes_common.py (satellite normalizer + maps + S3 list/download); _goes_archive_core + goes_archive/goes_animation/glm re-pointed there; GOESError base -> FetchError (invoke passthrough preserves BBOX_REQUIRED/GOES_INPUT_INVALID/GOES_EMPTY); StormTracks*Error -> hooks/storm_tracks.py base FetchError. Two new result models -> contracts LAYER_RESULT_MODELS (StormTracksLayerURI, GOESSatelliteLayerURI). Twins DELETED (storm 1,251 + goes 854); tests -> test_router_storm_tracks.py + test_router_goes_satellite.py; sfincs_forcing_autowire re-pointed to the registry closure (import-only, no flood seam, no canary). Baseline EXACTLY 9 by SET; catalog_surfacing spec-count pins 92->94 / 91->93; retrieval unshifted (both docstrings byte-verbatim, corpus queries top-8); daemon boot clean. Only nwm remains for the last finale wave. |
| 2026-08-04g | 83 (-1) | 5 (-1) | 92 (+1) | ~35,700 (twin -1,587; channel/hook +~980 NEW reusable capability) | FETCHER FINALE WAVE 1 (ADR 0110): fetch_topobathy FOLDED via the FETCH-TIME PROVENANCE CHANNEL. coded tools 84->83, coded fetchers 6->5, spec-served 91->92; campaign coded-data-fetcher counter 4->3 (target). Registry 173 UNCHANGED (twin died, spec took the name). Built the ONE general capability ADR 0089 named: a cache-replayable sidecar from fetch to envelope -- read_through binds a ProvenanceRecorder around the fetch, record_provenance() writes the four fields, the cache persists a <key>.provenance.json sibling + REPLAYS it on a hit, route() hands it to the envelope hook (additive: recorder=None -> byte-identical for all 91 priors, proven by the offline suite + a dedicated no-op test). fetch_topobathy folds onto a library_delegate raster spec + topobathy.validate/read/envelope hooks (4-leg CUDEM->regional->ETOPO->3DEP-land UTM warp-merge + NAVD88 datum gate; COG re-encode = accepted ADR-0074 divergence class). TopobathyResult -> contracts LAYER_RESULT_MODELS; TopobathyError -> hooks/topobathy.py base FetchError (invoke passthrough preserves TOPOBATHY_DATUM_MISMATCH). LOUD-FALLBACK NORM applied (0091 follow-up): land leg's silent swallow -> LABELED land_absent degrade; CUDEM->ETOPO warning channelled to the envelope every path. Twin DELETED (~1,587 LOC); tests -> test_router_topobathy.py + test_provenance_channel.py (channel proofs: cache-hit replay identical, land_absent labeled degrade). Consumers re-pointed (flood.py shim + inundation/wave_field/coastal-test). Baseline EXACTLY 9 by SET; [fetch_topobathy-topobathy] gate member identical pre/post; retrieval unshifted (docstring verbatim, top-8); daemon boot clean. Storm_tracks/goes_satellite/nwm (next finale waves) inherit the channel for free. |
| 2026-07-30 (current) | 176 | 85 | 14 | 62,665 (of 67,056 fetchers-pkg; router+transport engine = 4,391) | 14 sources debug through ONE 4,391-line engine instead of ~14k lines of bespoke files |
| 2026-07-30 | 176 | 85 | 14 | 62,665 | observability batch (ADR 0051): +902 LOC of NEW capability (rotation, retention, actionability, classifier); coded counts unchanged - infra landing, not a fold |
| 2026-07-30 | 173 | 82 | 17 | ~60,470 | fold wave 6 VECTOR family (ADR 0052): levees + slr_scenarios + ejscreen folded (-3 coded, -2,196 twin py), router gains declarative fan-out + float_list, endpoint_by_param/properties_by_param sub-layer routing, esri_json ingest + percentile/fraction/raw kinds + from_param + lowercase-enum; all 3 byte-identical live; ZIP family (ghsl/admin/storm/river) deferred by stop-rule (ghsl inner tif is DEFLATE not stored -> no windowed zip-member read; others multi-file shapefile/GDB or bespoke parsers) |
| 2026-07-30b | 173 | 82 | 17 | ~60,500 | fold wave 6: -3 coded (slr, levees, ejscreen; net -2,000 LOC); fan-out + per-enum routing + esri-json modes; zip family deferred w/ evidence (DEFLATE member, sidecars, bespoke parsers) |
| 2026-07-30c | 170 | 79 | 20 | ~58,786 (router+transport engine ~4,765) | fold wave 7 raster enablers (ADR 0053): landfire + usfs (imageserver_export exportImage mode + all-nodata gate) + modis_lst (stac_float continuous-float: latest-item + DN scale/offset + two-tier PC sign) folded (-3 coded, -1,684 twin py); router gains 2 raster ingestion modes + transport get_bytes + param-keyed style/units + payload ceil; all 3 byte-identical live (lf 32/32, us 32/32, modis 34/34); copernicus_dem deferred (5-consumer _copernicus_dem_impl coupling) + enablers 3-6 (redirect/gzip, VRT-fanout, griddap, colormap-DSL) not attempted -- fewer-fully doctrine |
| 2026-07-30c | 170 | 79 | 20 | ~58,900 | fold wave 7: -3 coded (landfire, usfs, modis via imageserver_export + stac_float); copernicus deferred (5-leg delegate re-point = scoped job); enablers 3-6 to wave 8 |
| 2026-07-30d | ~204600 | - | - | - | - | - | 190 | 13,116 | fold wave 8 (ADR 0054): copernicus_dem (stac_float + serialize-nodata; 5-leg re-point onto registry seam) + gcn250_curve_numbers (direct_window: transport skip-HEAD recovery + redirect-follow, enum-URL, integer-pixel window rounding, all-nodata gate, int16 serialize) folded (-2 coded, -1,053 twin py); both value-identical live (cop 13/13, gcn250 22/22 across 3 AMC); coded 170->168, fetchers 79->77, spec-served 20->22; chirps(gzip-object)/hrsl+soilgrids(VRT-fanout)/noaa_sst(griddap; ERDDAP unreachable in-env)/jrc(colormap-DSL) deferred by stop-rule; FLOOD CANARY PASSED |
| 2026-07-31 | 168 | 77 | 22 | ~57,900 | fold wave 8: -2 coded (copernicus via stac_float + 5-leg re-point, gcn250 via skip-HEAD/redirect); flood canary green; chirps/VRT/griddap/jrc stop-ruled |
| 2026-07-31b | 166 | 75 | 24 | ~57,170 (raster_cog +331 NEW capability) | fold wave 9 (ADR 0055): hrsl_population (multi_url VRT fan-out: whole-object .vrt parse -> intersecting-member windowed reads through the transport -> mosaic; all-nodata EMPTY, member-failure UPSTREAM) + chirps_precipitation (gzip_object: whole-object GET + gunzip + in-memory window; date-templated monthly/daily URL, sentinel-collapse, 404 NOT_AVAILABLE, bbox=None global) folded (-2 coded, -1,065 twin py); both value-identical live (hrsl 17/18 maxabsdiff=0.0, chirps 36/36 incl. global grid); coded 168->166, fetchers 77->75, spec-served 22->24; soilgrids stopped (projected-VRT Homolosine reproject + per-property scale-by-param does NOT stack on serialize/scale directives -> scoped job); griddap HELD (ERDDAP unreachable in-env), jrc DEAD; no flood coupling (grep-verified, canary not run) |
| 2026-07-31c | 163 | 72 | 27 | ~55,000 | fold wave 10 (ADR 0056): tier-3 hook contract (2 pure extension points, post_process rejected) + http_json executor; earthquakes/tsunami/volcano folded (-3 coded, net -1,500); remaining-72 ledger: ~15 tier-2-able, ~14 hook-needing, ~43 scoped-job |
| 2026-07-31b | 163 | 72 | 27 | ~54,400 | fold wave 11 (ADR 0059): copernicus absorbed (new internal tier, declarable -1, fold-zero on coded count); ALL 15 tier-2 candidates DEFERRED - wave-10 ledger optimistic, each needs a NEW mode (OGC-API vector, list-enum IN(), batched station, WCS, mapserver RGBA, tile_grid) |
| 2026-07-31d | 197400 (est) | 67 | 29 | ~53,000 | hook wave (nws_event+usace_nsi via hooks, -2 coded) + credentials wave (QGIS-native collapse, vault deleted, -954 server LOC, resolver+auth_broker); telemac diag test = new documented order-flake; merged clean 9==9 |
| 2026-07-31e | ~193000 | 63 | 33 | ~48,500 | chained-resolution mode (ADR 0063): 1 mode -> 4 folds (gbif, inaturalist, nws_alerts, nws_river_forecast); ratchet promoted; flood canary green; net -4,600 LOC |
| 2026-07-31f | ~190600 | 61 | 35 | ~45,300 | ratchets wave (ADR 0064): openfema + storm_events folded with ZERO new engine machinery (phase composition proven); probe_point relocated, URI utils hoisted; Cloud Run binding = live qgis_process seam (rejected as dead) |
| 2026-08-01 | 146 | 56 | 40 | ~39,000 | station-siblings wave (ADR 0065): the THIRD phase-composition proof. asos + raws + snotel + airnow + openaq folded via the EXISTING phases with ZERO new machinery (resolve/main-fetch/enrich/offset-paging + output.bbox_from_features + the keyed missing-key path). Keyless 3 value-identical LIVE (asos 179 obs, raws 790 obs, snotel 18 stations + station extent); keyed 2 missing-key + input byte-identical (AIRNOW_MISSING_KEY / OPENAQ_KEY_REQUIRED credential-shaped), data path blocked-on-live-key (never register a real key), openaq join proven offline. coded 151->146, fetchers 61->56, spec-served 35->40; registry unchanged (5 twins died, 5 specs took names); 7,746 twin+test LOC removed, +1,206 hook LOC; retrieval UNSHIFTED (37/38 top-8, docstrings verbatim); suite 9==9; NO new HookSpec field / mode / transport capability; NO flood coupling (grep-verified, canary not run) |
| 2026-08-03 | 98 | 11 | 88 | ~38,000 | animation wave 2 (ADR 0088): netcdf_cf_object per-frame builder (hooks/goes_archive.py + imagery/_goes_archive_core substrate) folds fetch_goes_archive_animation + fetch_goes_active_fire onto shape:animation_frames -- ONE frames_plan/frame_bytes pair, per-frame cache_params BYTE-identical, docstrings VERBATIM. coded fetchers 13->11 (campaign coded-data-fetcher counter 11->9), coded tools 100->98, spec-served 86->88; registry UNCHANGED 186 (2 twins died, 2 specs took names). Two additive FramePlan carriers (fetch_context out-of-cache-key S3 key + per-frame style_preset), strict no-op for priors. LIVE: 3-frame Fire-Temp archive animation (Utah, one scrubber group, 3-band COG readback) + 3-frame split-window active-fire (N.California, real hot pixels) on trid3nt-cache. Retrieval unshifted (3/3 top-8 both). goes_satellite STOP (single-band float32 raster-cog + em-dash name + most-recent semantics) + glm STOP (single-vs-list output variant) with refined residuals. |
| 2026-08-03b | 98 | 11 | 88 | ~38,000 | topobathy fold wave (ADR 0089): fetch_topobathy STOP re-affirmed + SHARPENED -- NO fold, docs-only. Campaign coded-data-fetcher counter UNCHANGED at 9 (target 9->8 honestly deferred); coded tools/fetchers/spec-served/registry all unchanged. Decisive new finding: a fold fails GATE 1 (edge-matrix parity) because TopobathyResult's 4 fields are FETCH-TIME provenance (which of 4 sources painted the merge), unrecoverable from the final COG, and the router's envelope hook is PURE-over-final-bytes with NO fetch_fn->envelope provenance channel (cache hit never runs fetch_fn). Delegate-hook escape = forced relabel of ~600 LOC + STILL fails GATE 1. fetch_dem still a coded twin (no dem spec -> no route()-recursion on the topo leg). Flood seam UNTOUCHED (no re-point) so NO flood canary mandated. Offline baseline unchanged by construction: 9 failures. |
| 2026-08-03d | 98 | 11 | 88 | ~38,000 | gated-DEM-fallback wave (ADR 0091): BEHAVIOR change inside fetch_dem, NOT a fold -- coded tools 98 / coded fetchers 11 / spec-served 88 / registry 186 all UNCHANGED. The auto path no longer silently swaps 3DEP (US 1-10 m lidar) -> Copernicus GLO-30 (global 30 m radar); on a 3DEP outage it raises the user-gated DemAutoFallbackGateError (DEM_FALLBACK_GATE, retryable, .suggestions naming source="copernicus" + the lidar/radar tradeoff), and a non-US bbox raises the DISTINCT DemOutOfCoverageError (DEM_OUT_OF_COVERAGE) before any 3DEP attempt (generous CONUS+AK+HI+PR envelope). Mechanism = typed retryable error riding the tool-retry loop (NOT the #154 granularity confirm card -- the swap is only knowable AFTER 3DEP fails at runtime, not a pre-dispatch param). 8 direct importers audited: all PROPAGATE honestly (flood.py -> failed AssessmentEnvelope solver_version=failed:DEM_FALLBACK_GATE; compute_contours unwrapped rise; elmfire/geoclaw/swmm/landslide -> typed workflow errors carrying the gate prose; extract -> PairingQuantityMismatchError w/ escape hatch); fetch_topobathy's best-effort land-leg swallow left as a named characterize-only FOLLOW-UP. Dissolves ADR 0090 fetch_dem blocker 1 (cross-tool provenance restamp). FLOOD CANARY: happy path status=ok + depth COG published (gate invisible, fallback_note=null); forced-failure drive proved gate text + honest propagation end-to-end. Offline baseline unchanged: 9 failures (the [fetch_dem-dem]/[fetch_topobathy-topobathy] gate members fail identically pre/post). Retrieval unshifted (fetch_dem 16/16 corpus queries top-8). |
| 2026-08-03c | 98 | 11 | 88 | ~38,000 | DEM+STORM_TRACKS wave (ADR 0090): fetch_dem + fetch_storm_tracks BOTH STOP -- NO fold, docs-only. Campaign coded-data-fetcher counter UNCHANGED at 9 (target 9->8/7 honestly deferred); coded tools 98 / coded fetchers 11 / spec-served 88 / registry 186 all unchanged. fetch_dem = a 3DEP-primary + Copernicus-GLO-30-fallback multi-source COMPOSITE: (1) cross-registered-tool fallback ladder with fetch-time provenance restamp (name suffix + LayerURI.fallback_note) -- the ADR 0089 envelope-provenance gap (router fallback surfaces are same-source mirror/parse chains, no cross-tool restamp, cache-hit never runs fetch_fn) -> GATE 1 fail; (2) bespoke 3DEP leg = py3dep.get_dem (seamless path, NOT pfdf) + bounded-timeout DAEMON thread (DemPrimaryTimeoutError) + reproject-bounds partial-coverage gate (DemPartialCoverageError, retryable) -- I/O steps a PURE hook (ADR 0056) cannot carry; (3) source-enum pin + pixel-budget auto-coarsen; (4) WIDE 8-consumer direct-import blast radius (flood.py + fetch_topobathy nested + 6 more). Does NOT unblock the ADR 0089 topo leg (partial dem spec already = fetch_copernicus_dem + fetch_3dep_extra; the missing py3dep-seamless leg would not reproduce fetch_dem's full fallback+coverage contract). fetch_storm_tracks: the ADR 0089 "track/line assembly" bucket is STALE (fetch_movebank_tracks already folded -> LineString grouping PROVEN foldable), and HISTORICAL IBTrACS mode alone WOULD fold on http_json + hooks like movebank; DECISIVE BLOCKER = the single-named tool must reproduce BOTH modes, and ACTIVE mode is a resolve-then-fetch chain whose SECOND round is a BINARY zip-shapefile (NHC forecastTrack zip -> tempdir extractall -> geopandas.read_file -> reproject), I/O inside the PURE enrich_merge hook -> GATE 1 fail. Only sfincs_forcing_autowire imports it directly (narrow); no seam re-pointed -> no canary. Offline baseline unchanged by construction: 9 failures (test_fetch_resolution_gate x4 [incl [fetch_dem-dem], a pre-existing member, failing identically] + test_run_river_dye_scenario x5). |
| 2026-08-03f | 96 | 9 | 90 | ~37,500 | LLM-surface dedupe (ADR 0093): docstring-only, ZERO tool-count change (coded tools 96 / coded fetchers 9 / spec-served 90 / registry 186 all UNCHANGED). Census verdict: the docstring corpus is ALREADY deduplicated (ADR 0057 legacy) -- cross-docstring repetition is legitimate per-tool contract (own cache class / tier-key / typed error), NOT doctrine. The one genuine zero-loss dup = the engine-door return-envelope enumeration, byte-identical across all 9 doors + re-delivered by the runtime envelope. Trimmed from all 9 door function docstrings. Fed-surface delta (Bedrock <=1000 desc): 9 doors 8608->6794 chars (-1814, ~-454 tok); run_sfincs (only door in the always-visible HOT_SET floor) 1005->797 (-208 chars off EVERY turn); full 186-tool catalog 184222->182408 (-1814); system prompt UNCHANGED (nothing hoisted -- the info already lives at runtime). Retrieval unshifted for all 9 doors (1550-pair corpus; 1 non-door boundary flip on fetch_usace_nsi = global BM25 renorm, within-noise, documented). Live smoke green (run_sfincs + run_pelicun round-trip, envelopes still carry all 6 trimmed keys). System-prompt internal redundancy (intentional emphasis + test-pinned) STOP w/ named residual per scope-6. Offline baseline unchanged: 9 failures. |
| 2026-08-03e | 96 | 9 | 90 | ~37,500 | approved-folds wave (ADR 0092): fetch_population (WorldPop leg via library_delegate raster: worldpop.validate pre-cache vintage gate + worldpop.read whole-object-download-then-window socket; the half-built ACS leg DROPPED -> acs_* is a typed input error, fetch_census_acs serves tract population) + fetch_glm_lightning (default output CONTRACT CHANGE layer->[layer]: shape:animation_frames via glm.frames_plan/frame_bytes, GLM-L2-LCFA GROUP-energy point-gridding numpy.add.at over imagery/_goes_archive_core grid+writer, single accumulation = one-frame list). coded fetchers 11->9 (campaign coded-data-fetcher counter 9->7), coded tools 98->96, spec-served 88->90; registry UNCHANGED (2 twins died, 2 specs took names). NO new access mode / SourceSpec field / HookSpec field -- both reuse proven modes (library_delegate raster, animation_frames). -1206 twin py + -694 twin-test py; +721 hook py + 247 spec YAML + 643 router-test py. compute_exposure_summary._fetch_population_layer re-pointed to the registry closure (reads only .uri; consumer suite green). Retrieval unshifted (both names top-8 for their corpus queries). Offline suite baseline unchanged (9 failures). NO flood-seam re-point (grep-verified, canary not mandated). |
| 2026-08-03i | 85 | 7 | 90 | ~37,000 | dem-fold wave (ADR 0096): fetch_dem fold ATTEMPTED, STOPS -- docs-only, ZERO code change. coded tools 85 / coded fetchers 7 / spec-served 90 / registry 175 all UNCHANGED; campaign coded-data-fetcher counter UNCHANGED at 5 (target 5->4 honestly deferred, same disposition as topobathy/storm_tracks). BLOCKER 2 CLEARS: ADR 0090's "a hook is PURE (no I/O)" was over-broad -- the library_delegate DELEGATE hook is the sanctioned socket-owning impurity (ADR 0074), so the py3dep leg (py3dep.get_dem + bounded-timeout daemon-thread DemPrimaryTimeoutError + reproject-bounds DemPartialCoverageError coverage gate) ALL fold into a new py3dep.read delegate hook, sibling of pfdf_3dep.read (LZW/5070 vs router DEFLATE COG-bytes = the accepted ADR 0074 divergence class). DECISIVE STANDING BLOCKER = the source="copernicus" leg: the twin returns TOOL_REGISTRY["fetch_copernicus_dem"].fn(bbox) VERBATIM (copernicus_dem-cached uri + copernicus_dem layer_id/name), and the router has NO cross-registered-tool dispatch seam (route() emits ONE LayerURI stamped with THIS spec's source_class "dem"; access is a spec constant, no access_by_param; a delegate re-serializes under "dem" -> divergent uri/layer + double-caches; grep confirms zero cross-tool dispatch in _router/). LOAD-BEARING: DemAutoFallbackGateError/DemOutOfCoverageError NAME source="copernicus" as the user retry, so it MUST work on the same tool. Secondary: naming (dem-{lon}-{lat}-{Nm}/USGS 3DEP DEM (Nm)+coarsen note) needs envelope+result_model (a no-field DemLayerURI or a pairing relaxation); the Dem*Error classes (UpstreamAPIError, not RouterError) are wrapped by library_delegate.invoke's RouterError-only passthrough, dropping the test_data_fetch-pinned codes unless invoke broadens to except FetchError. UNBLOCK = a hooks.dispatch cross-sibling-source short-circuit (first tool-composes-tool router pattern, a NATE architecture decision against the atomic-tools doctrine) + the naming seam + the invoke FetchError passthrough = 3 new-machinery pieces = a scoped job, not a fold. Offline baseline unchanged by construction; captured test_fetch_resolution_gate = 4 failed / 19 passed ([fetch_dem-dem]/[fetch_topobathy-topobathy] fail identically, assert 'local'=='fetch', empty diff). No seam re-pointed -> no flood canary mandated. Registry imports clean (175, fetch_dem + fetch_copernicus_dem present). |
| 2026-08-03h | 85 | 7 | 90 | ~37,000 | combined hygiene wave (ADR 0095, four NATE-decided legs): (L1) "North Star" term BANNED -- reworded ~44 carriers across server/src + server/tests + services + non-ADR specs to plain description / "reference scenario"; the two OLDER ADRs (0024, 0041) that carry it are immutable and LEFT UNTOUCHED; retrieval-sensitive docstrings (fetch_topobathy, sfincs flood composer, adapter routing) reworded WITHOUT dropping domain keywords, model-free retrieval rank-stable (fetch_topobathy top-8 both coastal-bathymetry queries, identical retrieved sets pre/post). (L2) composer keep-vs-cull characterization (docs/validation/composer-cull-characterization.md, report-only): 3 KEEP + 7 CULL-CANDIDATE (the internal model_* siblings, each a 1:1 private body of one registered engine template); 7 QUEUED ledger rows, NATE decides. (L3) fetch_cama_flood_discharge DELETED (US-only doctrine + NWM covers US + dead gated upstream; supersedes ADR 0069/0078 HELD): module + test + corpus + registry/main/catalog/offload-set/adapter-enum/credential-map + all sibling docstring cross-refs. The generic SFINCS discharge_forcing_from_cama_cog sampler RETAINED but orphaned -> separate QUEUED ledger row (engine-seam excision deferred, canary-risky). (L4) fetch_nexrad_reflectivity RECLASSIFIED + RENAMED -> show_nexrad_radar: moved fetchers/weather -> new tools/display/ package (honest home for zero-byte WMS-URL overlays), docstring rewritten (composes a live WMS GetMap URL, fetches nothing), corpus re-authored, ALL consumers/tests re-pointed; ADR 0078 PERMANENT-BESPOKE verdict stays true, just no longer miscategorized. DELTAS: registry 176->175 in-process (daemon-startup 180->179; the 4 catalog_surfacing pins updated 180->179); coded tools 86->85 (cama -1; nexrad renamed not deleted); coded fetchers column 9->7 (cama deleted + nexrad reclassified out of the fetchers package); spec-served 90 UNCHANGED (cama was coded, not a spec). Campaign coded-data-fetcher counter 7->5 -- NEW BASIS: remaining coded data-fetchers IN the fetchers package = topobathy, dem, storm_tracks, goes_satellite, nwm = 5. Daemon boots clean (registry 179, declarable 178, index 178). Offline suite = EXACTLY the 9-failure baseline (test_fetch_resolution_gate x4 + test_run_river_dye_scenario x5); the transient 4 catalog_surfacing failures in the mid-edit full run were the stale 180 pins, GREEN on re-run (14/14). Contracts suite 695 passed. |
| 2026-08-03g | 86 | 9 | 90 | ~37,500 | door dissolution (ADR 0094, supersedes 0034): the 10 engine-door concierge tools (run_sfincs..run_pelicun incl run_modflow) DELETED; tier=template rejoins the retrieval pool. coded tools 96->86 (the 10 doors were coded; coded fetchers 9 + spec-served 90 UNCHANGED -- the 20 templates were already registered), registry 186->176. NET -3,992 LOC (door modules -2,557 incl door corpus.yaml; 10 door test files -1,554; +fidelity docstrings + sfincs corpus + new test_door_dissolution.py + ADR). RETRIEVAL: 20/20 templates surface top-8 model-free retrieve_visible_tools(q,None,8); pre-existing non-template surface rank-stable (own-corpus top-8 misses 15->14 under identical with/without-templates method, ZERO non-template regressions). SELECTION SAFETY sane (urban->swmm_urban_flood #1, coastal surge->sfincs_flood #1, seismic->openquake_psha #1, wildfire->elmfire_fire_spread #1; ambiguous flood surfaces the full flood family, system-prompt ladder arbitrates). Cross-engine fidelity ladder relocated ONCE into the system-prompt flood block (test-pinned, honesty rules tightened not weakened); each template docstring front-loads its own Fidelity+Off-scope line. HOT_SET run_sfincs->sfincs_flood. Gate-expansion machinery (_engine_door_tool_names/_DOOR_EXPAND_CAP/door-branch) removed. Daemon boots clean (registry 180 incl 4 startup-only, 0 doors, declarations build). Flood canary GREEN: direct-call sfincs_flood (Boulder CO AOI, 10yr/6hr) ran the full SFINCS solve, published flood_depth_peak.tif + animation frames + overview COG to s3://trid3nt-runs, no SOLVER_FAILED. Offline suite GREEN: 9 failed / 9145 passed -- EXACTLY the documented baseline (test_fetch_resolution_gate x4 + test_run_river_dye_scenario x5); the 6 door-era test failures (4 catalog_surfacing count pins, modflow template-category inversion, spatial_query registry floor) were updated for the new tool surface. |
| 2026-08-03j | 84 | 6 | 91 | ~37,150 | DEM completion wave (ADR 0097): fetch_dem FOLDED to a spec-driven library_delegate source. coded tools 85->84, coded fetchers 7->6, spec-served 90->91; registry 175 UNCHANGED (twin died, spec took the name); campaign coded-data-fetcher counter 5->4 (target hit). Built ADR 0096's three residual machinery pieces: (1) CROSS-SIBLING DISPATCH seam (contract DispatchSpec + SourceSpec.dispatch + router.try_dispatch) -- a spec-declared SINGLE-target pre-flight short-circuit, the FIRST tool-composes-tool router pattern (NATE-gated, deliberately narrow: one fixed target, spec-declared, NO chains, pre-flight-only); source="copernicus" returns fetch_copernicus_dem's layer VERBATIM (its copernicus_dem cache prefix + layer_id/name), NO dem-prefix double-cache. (2) library_delegate.invoke/resolve RouterError->FetchError passthrough so the Dem*Error pinned codes (DEM_FALLBACK_GATE/DEM_OUT_OF_COVERAGE/DEM_PARTIAL_COVERAGE/DEM_PRIMARY_TIMEOUT) survive; Dem*Error home = hooks/dem_3dep.py; no other delegate source's wrap-to-generic changed (pfdf/dataretrieval/HRRR router tests green). (3) a no-field DemLayerURI result_model + dem_3dep.envelope naming override (dem-{lon}-{lat}-{Nm}/USGS 3DEP DEM (Nm)+coarsen stamp) -- less invasive than relaxing the shared envelope/result_model pairing validator. The py3dep leg = dem_3dep.read delegate (get_dem + env-tunable TRID3NT_DEM_PRIMARY_TIMEOUT_S daemon-thread watchdog + reproject-bounds coverage gate + source-conditional gating auto->gate / pinned->plain suggesting error / partial->propagate); auto-coarsen = dem_3dep.coarsen pre_resolve (cache key twin-identical {bbox,resolution_m} when NOT coarsened; requested_res_m rides only when downsampling); continent ceiling + out-of-coverage = dem_3dep.validate. Docstring carried VERBATIM (index unshifted); metadata twin-identical (source_class=dem, static-30d, auto_publish=False). fetch_dem.py twin DELETED (~729 LOC); DEM tests migrated to test_router_dem.py (0091 pins intact); 8 consumers re-pointed to the registry closure (keyword-only -- the promoted **kwargs closure rejects positional; flood.py+compute_contours keep a module-level registry-closure indirection for the tests' module-attr patch seam). LOC: source ~+146 (the reusable dispatch machinery + verbose hook/spec offset the -729 twin), tests ~-126 (test_data_fetch -512 / test_router_dem +386). Offline baseline unchanged: [fetch_dem-dem]/[fetch_topobathy-topobathy] gate members fail IDENTICALLY (4 failed/19 passed, assert 'local'=='fetch', empty diff). Retrieval unshifted by construction (docstring byte-identical + corpus.yaml retained + tier=general). LIVE (MinIO + real network): source=3dep real 3DEP COG (81175 B); source=copernicus DISPATCH uri/layer_id/name byte-identical to a direct fetch_copernicus_dem call + ZERO new dem-prefix cache object (no double-cache); auto happy (no fallback_note); forced 3DEP failure -> DEM_FALLBACK_GATE end-to-end (no cache write). FLOOD CANARY GREEN: direct-call sfincs_flood (Boulder CO, 10yr/6hr) solved through the re-pointed fetch_dem seam -> flood_depth_peak.tif (9835 B) + 25 depth frames + F33 overview COG published to s3://trid3nt-runs. Benign divergence flagged: source is in the DEM cache key (twin excluded it), so source=auto and source=3dep key separately -- a cache-sharing micro-difference, never a wrong answer or a shared-mosaic double-cache. |
| 2026-08-03k | 84 | 6 | 91 | ~37,150 | mesh layer wave M1 EXTRACT (ADR 0098): refactor/extraction, ZERO tool-count change (coded tools 84 / coded fetchers 6 / spec-served 91 / registry 175 all UNCHANGED). NEW package server/src/trid3nt_server/agent/mesh/ (grid_geometry.py NEW 131 + mesh_preview grid-outline preview NEW ~155 + __init__ 48 + gwt_adapter _build_gwf_dis helper NEW ~51 = ~385 NEW component LOC). RELOCATED via git mv (history preserved, content byte-identical): swmm_mesh_builder.py 1785 -> mesh/raster_cell_mesh.py; shared/mesh_layer.py 348 -> mesh/mesh_preview.py (=2133 relocated). DELETED: CaMa forcing sampler + plumbing ~146 (RIDER, resolves ADR 0095 QUEUED row). +47 compat-shim LOC (2 full-namespace re-export shims at the old paths, DELETION_LEDGER QUEUED for M2 re-point). FOLDS: MODFLOW 5x ModflowGwfdis -> 1 _build_gwf_dis helper (value-identical .dis, in-file because gwt_adapter is imported FLAT server-side + PACKAGE worker-side); grid_geometry = the SERVER regular-grid math (SWAN/MODFLOW/SFINCS derivations stay worker-side behind the Docker COPY-services/workers-only boundary -- honest split, documented). NEW capability: SFINCS/SWAN/MODFLOW get their first publishable grid-outline preview. TELEMAC = mesh_preview generalization only (mesher 100% worker-side already). GATES: modflow worker deck tests 162 passed; mesh/swmm/granularity/forcing consumer tests green; offline baseline = the documented 9 failures; flood canary green. |
| 2026-08-04 | 84 | 6 | 91 | ~39,150 | oceanmesh wave (ADR 0101), 3 legs: worker + component code only, ZERO tool-count change (coded tools 84 / coded fetchers 6 / spec-served 91 / registry UNCHANGED; no registered tool, no new WS event, no contract enum). ROLLING LOC SPLIT ~+700 (WORKER ~407: NEW services/workers/mesh/ oceanmesh coastal_tin worker entrypoint.py 124 + coastal_tin_build.py 179 + __init__ 7 = 310, + telemac worker leg-1 edits ~97 [BanksUnavailableError, explicit bank_source mode+gate, forced-empty env seam]; SERVER ~316: NEW agent/mesh/coastal_tin.py 142 [thin M1/M2 dispatcher, offline-suite-safe] + leg-1 edits ~174 across model_river_dye_release_scenario +131 / river_dye +18 / run_telemac +4 / solver_confirm +11 / mesh __init__ +10). LEG 1 (BUILT+LIVE): explicit TELEMAC bank_source (nhd_area default | constant_ribbon) end-to-end + typed retryable TELEMAC_BANKS_UNAVAILABLE gate (DEM_FALLBACK_GATE pattern) replacing the silent NHDArea->ribbon fallback; provenance in mesh-gate stats + result envelope; live mesh_only drives GREEN (Columbia nhd_area real banks frac=1.00 3981 nodes / constant_ribbon ribbon 825 nodes / forced-empty -> exit-3 gate text); telemac image rebuilt (code-only, 3.55 GB unchanged); retrieval re-proof 7/7 top-8. LEG 2 (BUILT: generator+validation, NO solver consumes yet): NEW GPL-isolated mesh worker image (multi-stage python:3.11-slim, CGAL/GMP/MPFR + oceanmesh @v1.0.0 from CHLNDDEV github [NOT on PyPI], numpy<2 ABI pin); server coastal_tin.py composes the OceanMesh2D sizing SPEC (distance/feature/wavelength/slope + gradation) -> worker -> mesh_grid preview. IMAGE ~1.33 GB amd64 (oceanmesh venv closure 874 MB + trixie base ~140 MB + runtime libgmp/mpfr/expat ~2 MB + code 33 kB); hygiene multi-stage (CGAL/GMP/MPFR-dev + toolchain stay in the build stage, never runtime), libexpat1 for fiona. VALIDATION (paper-first, Galveston Bay TX on oceanmesh's own GSHHS_i_L1 + galv_sub fixtures): mean q_E=0.9572 MATCHES the User Guide's published 0.956-0.967, Eq.2 termination q_bar-3sigma=0.807 PASS (q_E computed as the scale-invariant Eq.1 equilateral quality in metric coords -- oceanmesh's own simp_qual is a scale-dependent radius ratio, NOT the paper's q_E). Spot-checks: Galveston TIN (12,159 verts) + Mexico Beach FL (858 verts, q_E 0.9515) rendered proofs. LEG 3 (report-only): schism-dev RiverMapper+pyDEM characterized -> QUEUED as bank_source="dem_derived" own wave (Apache-2.0/MIT, effort M). GATES: offline baseline = the documented 9 failures (5 river_dye members identical in kind: _fake_publish stale-mock TypeError 8->9 args from the legit provenance arg); telemac+mesh test sets 51 green. No flood-seam touch -> canary not mandated. |
| 2026-08-03m | 84 | 6 | 91 | ~38,450 | mesh layer wave M3 HECRAS WRITER + worker (ADR 0100): worker + component code only, ZERO tool-count change (coded tools 84 / coded fetchers 6 / spec-served 91 / registry 175 all UNCHANGED, byte-identical; no new WS event, no contract enum). NEW WORKER services/workers/hecras/ (+437 python: entrypoint.py 184 bind-mount geompre->unsteady->h5py metrics with a Finished-sentinel honesty gate since the engines exit 0 on solve error, muncie_smoke.py 157 gate driver, test_entrypoint.py 88, __init__ 8) + Dockerfile (143, multi-stage python:3.11-slim, HEC official Linux_RAS_v66.zip SHA-256-pinned download+verify, 6.6 engines RasGeomPreprocess/RasUnsteady/RasSteady + bundled Intel/MKL/rhel_8 libs, ras-commander MIT + h5py). NEW SERVER component: agent/mesh/hecras_geometry.py (+206, read_2d_flow_area_cells: geometry-HDF 2D flow-area cells -> EPSG:4326 mesh_grid preview, lazy h5py/pyproj = offline-suite-safe). IMAGE ~1.69 GB uncompressed amd64 (ras-commander closure 777 + HEC libs/MKL 599 + engines 170 + base 140); hygiene: multi-stage, .dockerignore, COPY --chmod (killed a dup 170MB binary layer), single-platform (no attestation bloat); MKL + ras-commander-closure trims CHARACTERIZED. MUNCIE GATE GREEN host + in-container: property tables bit-identical GUI-vs-Linux (max|diff|=0 on 1D XSEC + 2D Cells-Vol/Faces-Area elevation), volume accounting error 0.005835% (<0.05% tol), 2D max WSE 951.9 ft. WRITER: WRITE STOPPED (empirical: RasGeomPreprocess does NOT build 2D subgrid tables, RasUnsteady READBathymetry requires them = RASMapper/Windows-authored; from-scratch topology insufficient) -> template-first fallback proven by the Muncie gate; READ/preview half BUILT (the tractable, reusable component). PROOF: Muncie 2D flow area 5391 cells rendered over White River (bbox -85.39..-85.37, 40.19..40.20). Boot check registry 175. Offline suite: EXACTLY the documented baseline, no order-flake, zero new failures. Flood canary NOT mandated (isolated new worker + read-only component, no flood-seam touch). |
| 2026-08-03n | 84 | 6 | 91 | ~38,450 | template fetcher-wiring wave 1 (ADR 0102): wire the audit's have-but-not-wired fetchers so baked physical constants become real fetched data + a typed gate fallback (ADR 0091). ZERO tool-count change (coded tools 84 / coded fetchers 6 / spec-served 91 / registry 175 all UNCHANGED, byte-identical -- params added to EXISTING tools, no new registered tool). 5 targets: (1) GeoClaw dam-break -- fetch_usace_dams (seam-1) via NEW workflows/geoclaw/nid_dams.py (+~215, lazy geopandas/boto3): dam location+height from NID (by dam_name / nearest-AOI, DAM_HEIGHT ft->m + NID_STORAGE) -> GeoClawDepthLayerURI.source_note (NEW additive field); no dam + no user source_lonlat+dam_break_depth_m -> typed GEOCLAW_DAM_INPUT_REQUIRED; tool dam_break_depth_m default 10.0->None sentinel + NEW dam_name param. (2) SWMM urban_flood -- silent 120 mm Atlas-14 fallback -> typed SWMM_PRECIP_LOOKUP_FAILED gate naming total_rain_depth_mm; lookup switched to seam-1; the 120 mm builder literal relabeled direct-call-only. (3) Landlab -- triggering rainfall from Atlas-14 (seam-1): overland intensity=depth/dur, landslide recharge=design-storm total as 1-day pulse (REJECTED the depth*24/dur burst -> ~900 mm/day over-saturation); failed lookup -> LANDLAB_RAINFALL_INPUT_REQUIRED; SOIL block STAYS demo (missing-fetcher half) + labeled in NEW LandlabSusceptibilityLayerURI.source_note; NEW rainfall_return_period_yr param. (4) TELEMAC river dye -- carrier discharge from NOAA NWM (fetch_noaa_nwm_streamflow, seam-1) at the reach seed -> reach["inflow_q_m3s"] (nearest-reach streamflow_cms); miss -> TELEMAC_DISCHARGE_INPUT_REQUIRED; NEW discharge_m3s param; INDEPENDENT of the ADR 0101 bank_source seam (worker 250 default + width-heuristic superseded, untouched). (5) SFINCS nws -- CHARACTERIZE+SKIP: the composer handles only Flood/Flash-Flood warnings + forces SFINCS with REAL OBSERVED MRMS QPE; CO-OPS surge is out-of-scope (no coastal-warning type) not a wiring gap -> no flood-seam touch, canary NOT mandated. Two additive contract fields (source_note x2, both None-default, no enum grown). Contracts suite 695 passed; changed-engine targeted 356 passed + landlab 46 passed. Retrieval unshifted (geoclaw/swmm/landlab/telemac templates top-8 for their queries). LIVE fetch/manifest proofs (MinIO env): GeoClaw Folsom Left Wing 44.2 m/145 ft/1.12M acre-ft + ocean-bbox gate; SWMM Atlas-14 289.6 mm Houston; Landlab Atlas-14 74.9 mm -> 37.5 mm/hr / 75 mm/day; TELEMAC NWM 230.6 m3/s Snake reach + explicit override; full solver solves not run this wave (the engines' solve paths unchanged). Offline suite: EXACTLY the documented 9 baseline (fetch_resolution_gate x4 + river_dye x5); river_dye identical-in-kind (4 byte-identical, test_tool_rejects_both shifts its unexpected downstream code STAGING_FAILED->DISCHARGE_INPUT_REQUIRED, still failing, count preserved; _install_composer_mocks gained a _resolve_reach_discharge fake). Flood canary NOT mandated (grep-verified no flood.py/sfincs_builder seam touched). |
| 2026-08-03l | 84 | 6 | 91 | ~37,800 | mesh layer wave M2 GENERALIZE (ADR 0099): refactor + components, ZERO tool-count change (coded tools 84 / coded fetchers 6 / spec-served 91 / registry 175 all UNCHANGED; no new WS event, no contract enum grown). NEW server components: spatial_roles.py (507, the canonical 7-role vocabulary barrier|breakline|breach|refine_region|aoi_clip|boundary|point+line, aoi->aoi_clip alias, honesty floor preserved) + refine_regions.py (144, per-region quadtree sizing spec) + preview_gate.py (181, shared mesh preview/approve gate riding the EXISTING PayloadWarningEnvelope+GranularitySuggestion spine; tin=ON/regular=OFF default, mode-as-parameter for the later input-gate wave). gates/spatial_input.py -> thin backward-compat adapter over spatial_roles (surface preserved); gates/cards/spatial_input.py surfaces breach/refine/breakline/boundary roles. WORKER: gwt_adapter +214 DISV/gridgen generator (gridgen_available/GridgenUnavailableError/_disv_refinement_features/_build_disv_gridprops/_build_gwf_disv + capture_zone opt-in refine_regions seam; DIS default byte-identical). +445 new tests (spatial_roles/refine_regions/preview_gate/disv_grid). M1 CARRYOVER: 2 compat shims DELETED (-47, ledger CONDITION-MET), ~15 consumers re-pointed, agent/mesh<->workflows.swmm import cycle broken (lazy swmm_hyetograph + dead load_manning_mapping re-export removed). STOPS: DISV live leg gated on the gridgen binary absent from the modflow image (GridgenUnavailableError names the Dockerfile condition; ~2-4MB curl+unzip layer + WEL/CHD/PRT vertex-cellid port); aoi_clip SFINCS characterized (worker include_mask seam in _sfincs_build/deck.py, NOT wired -- flood-canary-mandatory worker change); TELEMAC barrier characterized (gmsh hole/breakline, M4-adjacent); _build_telemac_mesh_envelope fold onto shared gate deferred (live-drive gated). GATES: 82 SWMM/mesh consumer tests + 25 new component tests + 140 modflow deck (13 mf6-skip) passed; live proof #1 (drawn wall via generalized parser -> build_swmm_mesh omitted 1 overland edge 761->760 + 400-cell preview) PASS in-process. Flood canary NOT mandated (no flood-seam file touched). Offline suite: 9 failed / 9131 passed / 22 skipped / 1 xfailed = EXACTLY the documented 9-failure baseline (test_fetch_resolution_gate x4 + test_run_river_dye_scenario x5), no order-flake tripped, zero new failures. |
| 2026-08-04 | 168433 | - | - | - | 14447 (-87) | 6706 | - | - | ADR 0103 plugin-repo cull + de-noise: REMOTE/Cognito mode removed (plugin_settings/dock/layers/settings_dialog net -87), daemon /plugins/ replaced by metadata-driven /plugin-repo/ (package_plugin.sh + Makefile wiring), 2 chat notes removed, ws_bridge signal-signature test added; suite baseline 9 |
| 2026-08-04b | 168817 (+384) | 131356 (+330) | 12359 | 34299 (+92) | 14447 | 6248 | 175 | - | ADR 0104 live-drive fixes (6 bugs): registry 175 byte-identical (no tool added/removed). Bug1 TELEMAC degenerate-reach gate (ReachDegenerateError, aspect<2) + hard mesh watchdog (build_channel_mesh_guarded = killable child-proc SIGKILL, dead SIGALRM removed) -> server TelemacReachDegenerateError. Bug2/3 SWMM subprocess isolation (_swmm_solve_subprocess.py NEW +75; run_swmm_deck solve moved to killable child, deadline=est*4 clamp[300,3600] env SWMM_SOLVE_TIMEOUT_S -> SWMM_SOLVE_TIMEOUT; single-instance lock dead). Bug4 stale positional fetch sigs -> keyword (SWMM DEM+buildings, +SFINCS obstacles); LOUD fallback labels (_urban_envelope_suffix). Bug5 SWMM END clock rolls past 24h (no strptime crash). Bug6 oil-slick upload-before-register (_s3_object_exists HEAD guard). +12 targeted tests. Offline suite baseline preserved 9-by-SET. Worker image rebuilt. |
| 2026-08-04c | 81 | 6 | 91 | ~34,300 (net -5,378 working-tree: +7,787 / -13,165) | composer dissolution (ADR 0105), two moves one wave. PART A: 7 overfit-era model_* scenario BODIES folded into their engine template modules (one honest file each), scenario-era names dropped -> generic model_<engine>_<template>: fire_spread->elmfire_fire_spread, dambreak_geoclaw->geoclaw_inundation, landslide->landlab_susceptibility, seismic_hazard->openquake_psha, wave->swan_wave_field, urban_flood_swmm->swmm_urban_flood (RENAME model_urban_flood_swmm->model_swmm_urban_flood), river_dye_release->telemac_river_dye. Folds are module consolidations: registered tool names/docstrings/params BYTE-IDENTICAL, envelope shape unchanged, ZERO coded/registry delta (bodies were unregistered; templates preserved). Importers re-pointed: solver_confirm SWMM+TELEMAC preview imports, 3 shared test files, per-engine tests, SOLVER_WORKFLOW_REGISTRY labels, scripts/run_*_direct dev drivers. PART B: 3 registered standalone composers DELETED outright (run_model_nws_flood_event_scenario, run_model_groundwater_contamination_scenario, compute_impact_envelope) -- coded tools 84->81 (-3), coded fetchers 6 UNCHANGED, spec-served 91 UNCHANGED, registry 175->172 in-process (daemon-startup 179->176; the 4 catalog_surfacing pins updated 179->176). Removed: modules + registrations + corpus + categories + scenario_reuse + tool_catalog_http + the server confirm-gate groundwater branch + the ImpactPanel _maybe_emit_impact_envelope WS emitter + docstring cross-refs + tests (4 deleted, 5 migrated/updated). JUDGMENT RELOCATED (verified the model still sees it): NWS alert-type->flood-mode routing (filter Flood/Flash-Flood Warning, warning-polygon AOI, observed MRMS precip, sfincs_flood, honest no-warning degrade) -> system prompt + fetch_nws_alerts_conus docstring; Invariant-9 never-invent-contamination-params -> system prompt + retained in the modflow_contaminant_plume docstring. Retrieval re-proof: 3 deleted names GONE from the index; modflow_contaminant_plume #1 for the spill-plume query, pelicun_damage_assessment surfaces for damage/loss, modflow_river_seepage #1 (docstring changed) rank-stable, live-flood query surfaces fetch_nws_alerts_conus + sfincs_flood. Daemon boots clean (registry 176). LOC split: Part A folds relocate ~5,000 LOC into door files (net ~0); the net -5,378 is Part B (composers 2,634 + tests 2,518 + impact-envelope machinery + ref cleanup). Offline suite: documented 9-failure baseline preserved by SET (river_dye x5 identical-in-kind + fetch_resolution_gate x4). |
| 2026-08-03d | 166,252 | 128,990 | 12,492 (+133) | 34,299 | 14,447 | 6,248 | 172 | - | ADR 0106 structured input provenance (WAVE 2): contract-additive only -- SyntheticInput model + render helper + LayerURI.synthetic_inputs + ImpactEnvelope.synthetic_inputs (contracts +133). Registry 172 byte-identical (no tool added/removed); CODED tools UNCHANGED. Populated 6 templates (geoclaw/landlab/asr/telemac/swmm/pelicun); 3 narration-seam fixes + general prompt rule. Offline suite EXACTLY 9 by SET (fetch_resolution x4 + river_dye x5), kind-identical. Retrieval unshifted (no docstring changed). |
| 2026-08-04d | 166,922 (+670) | 129,278 (+288) | 12,508 (+16) | 34,301 (+2) | 14,447 | 6,248 | 172 | - | ADR 0107 two-mode INPUT_REQUIRED gate (review-before-run). Registry 172 byte-identical (no tool added/removed); CODED tools UNCHANGED -- 7 existing tools gain the `input_mode` param (+ openquake `vs30`), docstrings deliberate. Additive contract: PayloadWarningEnvelopePayload.synthetic_inputs + OpenQuakeRunArgs.reference_vs30_ms (default-matching, deck byte-identical). NEW: agent/gates/pending.py (registry extracted from server so an in-tool gate rides the SAME #154 spine) + agent/gates/input_review.py (the shared helper: mode lever + review envelope + 3-round provide-values loop + fail-open no-session) + emitter.send_envelope. Wired 5 templates (geoclaw/asr/landlab/telemac/swmm) + openquake vs30 vertical; mesh preview gate threads the same lever. System-prompt review instruction (test-pinned). Offline suite baseline preserved 9-by-SET (fetch_resolution x4 + river_dye x5). Flood/SFINCS seams UNTOUCHED (grep-verified; canary not required). SWAN boundary + remaining 13 templates on the adoption path. |
| 2026-08-04e | - | - | - | 34,393 (+92) | - | - | 172 | - | ADR 0108 pysheds terrain-hydrology + reach-selection fix. Registry 172 UNCHANGED, CODED tools UNCHANGED: the pysheds primitives (delineate_watershed + extract_stream_network, sharing processing/_hydrology_common.py) were ALREADY registered/coded within the 172 -- this wave formalizes them (ADR 0108) and lands the ADR 0104 Bug-1 reach-selection residual. WORKERS +92: services/workers/telemac/telemac_river_dye_build.py `_mainstem_flowline_seed` (name-free NHDPlus_HR streamorde/totdasqkm mainstem re-seed, bounded 6 km, fail-open) wired into fetch_river_centerline for the no-river_name case. Live: Longview city seed raw-snaps COMID 24521434 (the exact 292 m Cowlitz stub ADR 0104 named); re-seed resolves COMID 24520446 (Columbia mainstem, order 11) from both the city + release seeds. pure resolve_centerline_seed UNTOUCHED (17 offline tests pass). pysheds via pfdf==0.4 transitive pin (pyproject note); numba first-call = ~2.2 s lazy import (first CALL, not boot) + <0.1 s JIT, no warm-up. HAND raster DEFERRED (open issue -> HEC-RAS). Offline suite baseline preserved 9-by-SET (fetch_resolution x4 + river_dye x5). Flood/SFINCS seams untouched (no canary). |
| 2026-08-04f | +1,216 | +314 | +175 | +439 (deck_edit 156 + entrypoint +130 + solved fixture) | - | - | 173 (+1) | - | ADR 0109 HEC-RAS engine landing (engine #11, template-first). Registry 172 -> 173, CODED tools +1 (the `hecras_muncie_flood` template). ONE registered archetype: the Muncie-class 1D/2D riverine-flood demonstration (White River, Muncie IN). Reparameterizes the unsteady inflow hydrograph (`.bNN` flow scaler -- the AUTHORITATIVE deck edit, pinned empirically: RasUnsteady reads flow from the .bNN, NOT the plan HDF); `flow_scale` multiplier (user/default) + `target_peak_cfs` (seam-1 fetcher path), clamped [0.25, 4.0]. New: contracts/hecras_contracts.py (HECRASRunArgs + HecrasDepthLayerURI + 4 typed error codes); workers deck_edit.py + entrypoint deck-staging/flow-scale (+130); server pkg workflows/hecras/ (run_hecras local-docker spec + postprocess_hecras depth-COG+mesh-preview + muncie_flood template + _template_card); wired workflows/__init__ + tools/__init__ + categories (hazard_modeling). Depth raster HONESTLY REUSES continuous_flood_depth. Demonstration-geometry honesty LOUD (docstring fidelity line + envelope fallback_note; off-scope -> sfincs_flood). CANARY GREEN in-container (real RasUnsteady): 1.0x peak 21000 cfs depth_max 20.24 ft ~4881 wet cells vol_err 0.0058% + 1.3x peak 27300 cfs depth_max 20.62 ft ~4998 wet cells vol_err 0.0056% (higher flow deeper+wider; both inside M3 <0.05% gate). Retrieval: hecras_muncie_flood surfaces for all 5 corpus queries. Image 2.2 GB unchanged (tiny code layer; compact solved TEST fixture .dockerignore'd). Offline suite baseline preserved 9-by-SET (fetch_resolution x4 + river_dye x5). Flood/SFINCS seams untouched (grep-verified; hecras acceptance IS the canary). |
| 2026-08-04g | +0 | +0 | +0 | +516 (deck_quadtree.py) | - | - | 173 | - | ADR 0113 M4 QUADTREE TRUTH: the real SFINCS quadtree leg. Registry 173 byte-identical (ZERO tool added/removed; the quadtree flag rides existing sfincs_flood/model_flood_scenario). CODED tools UNCHANGED. Tooling: cht_sfincs==1.0.0 (GPL-3, Deltares Coastal Hazards Toolkit) -- hydromt_sfincs CANNOT author a quadtree from scratch at ANY version (setup_grid `# TODO gdf_refinement`, setup_dep NotImplementedError; changelog v1.2.0 "reading/writing NOT building", newest v2.0.0-rc3 unchanged). NEW services/workers/_sfincs_build/deck_quadtree.py (+516): build_sfincs_quadtree_deck(spec,scratch,download) -- refined quadtree (coarse offshore -> fine at the shore + drawn refine_regions), per-face bed sampled from OUR topobathy COG (no cht_bathymetry), auto seaward open-water boundary (lowest-mean-bed edge), surge forcing (fetched CSV else design surge), sfincs.inp+quadtree sfincs.nc+bnd/bzs+mesh.geojson preview, provenance -> 0106 synthetic_inputs. Self-contained cht import shim (_ensure_cht_importable injects sys.modules stand-ins for the OLD cht_utils API + unused cht_bathymetry/datashader) -> image needs ONLY cht_sfincs+tabulate --no-deps. Worker entrypoint branches on options.quadtree; validate_job_spec relaxed (quadtree needs dem_uri only). flood.py: phantom AWS-Batch DECK_BUILD_FAILED narrative DELETED (ledger), quadtree docstring+comments rewritten to the real worker build+solve contract. Dockerfile +cht_sfincs/tabulate/rioxarray/xugrid (--no-deps, ~+2.4 MB pip on the ~2.2 GB image; NOT rebuilt this wave). CANARY (offline, production module): synthetic Mexico Beach topobathy -> build_sfincs_quadtree_deck -> 5610 cells / 3 levels (200/100/50 m) / sea-edge auto "south" / 5575 active + 35 boundary -> SFINCS v2.3.3 solve 0.9 s -> face-indexed sfincs_map.nc (5610 faces), _is_quadtree_output True, peak depth 8.0 m over 2740 wet faces. Read-side probe (_is_quadtree_output/_read_face_coords/_select_peak_depth/rasterize_face_field) verified REAL, NOT bit-rotted. Retrieval re-proof: sfincs_flood top-8 for coastal-quadtree + hurricane-surge-Mexico-Beach + fine-cells-at-coast + pluvial queries (unshifted). Offline suite: 9 failed / 9054 passed / 17 skipped / 1 xfailed = EXACTLY the documented 9-failure baseline by SET (fetch_resolution_gate x4 + run_river_dye_scenario x5), zero new failures. DEFERRED: SnapWave coupling (snapwave_mask present), quadtree subgrid (needs cht_bathymetry), flood.py WS-driven live canary + local worker-image build+solve dispatch (worker-level canary exercises the identical chain). |
| 2026-08-04i | +1 tool | +1 test file (test_schism_landing.py, 15) | +schism_contracts.py | +14 (mesh worker npz emission) | - | - | 175 -> 176 (+1) | - | ADR 0118 SCHISM engine landing (engine #12, barotropic tidal archetype). Registry 175 -> 176, CODED tools +1 (`schism_tidal_hydro`). ONE archetype (barotropic tidal, NO external forcing legs), two mesh sources: `bundled_quarterannulus` (SCHISM's analytical M2 verification case -> RMSE gate) + `coastal_tin` (oceanmesh TIN + fetched bathymetry + constituent boundary -> clipped max-elevation COG). New: contracts/schism_contracts.py (SCHISMRunArgs + SchismElevationLayerURI + 4 typed errors); workflows/schism/ (run_schism local-docker spec + deck_authoring [QA fixture staging + tin_to_hgrid bridge loaded by-path + bathymetry sampling + bctides/param authoring] + postprocess_schism [out2d UGRID -> max-elev COG + mesh row + station chart + analytical verify] + tidal_hydro template + corpus); wired workflows/__init__ + tools/__init__ + categories (hazard_modeling). MESH EMISSION ROW (ADR 0116 handoff): additive `layer_type` gains "mesh" + `crs_authid` on LayerURI/ProjectLayerSummary/CaseManifestLayer, threaded through the WS row (add_loaded_layer -> ProjectLayerSummary.model_dump -> event.raw), plugin _add_mesh ready; SFINCS map-mesh retroactively emittable (NOT wired -- no flood-seam edit, no canary). Elevation raster HONESTLY REUSES continuous_flood_depth. ACCEPTANCE (a) GREEN LIVE: QuarterAnnulus through the REGISTERED TEMPLATE -- real docker solve (out2d_1.nc + staout_1 in MinIO) -> RMSE 0.01551 m (gate <=0.030), amp_err 0.0027 m (gate <=0.010), corr 0.99882, station amp 0.4393 m, tidal range 0.8805 m, 130 nodes -- matching the ADR 0115 spike numbers. Retrieval 6/6 corpus queries top-8. Offline suite baseline preserved 9-by-SET. Flood/SFINCS seams untouched (grep-verified; SCHISM acceptance IS the canary). ACCEPTANCE (b) coastal_tin: code-complete + unit-tested, live Galveston drive prepared for NATE (needs GSHHG shoreline + mesh image rebuild + topobathy fetch + multi-day solve). |
| 2026-08-04h | 167,596 (+134) | 128,988 (+203) | - | - | 14,746 (+299) | - | 173 | 12,817 (+134) | ADR 0114 !run chat invocation: direct tool dispatch from the composer. Registry 173 BYTE-IDENTICAL (no registered tool -- !run is a PROTOCOL feature). Client parse-first (NEW plugin/net/run_invocation.py 177, pure/no-Qt: ast eval-mode + ast.literal_eval per-kwarg SAFE parser + JSON-object form + is_run_prefix shared predicate) -> NEW WS type dev-tool-invoke {name,args,case_id,raw_text?} (no contract model, read defensively) -> server _handle_dev_tool_invoke reuses _dispatch_tool_and_persist -> _invoke_tool_via_emitter so gates (payload-warning/code-exec/solver) + _ALWAYS_OFFLOAD_SYNC_TOOLS offload + layer materialization + tool-io card + turn-complete ALL ride the model-call path. Unknown tool -> shared TOOL_NOT_FOUND. Attribution = persisted !run signature user bubble (raw_text). Blue !run token via QSyntaxHighlighter on _ChatInput firing the SAME is_run_prefix predicate as routing (test: highlight-on iff routes-direct). PLUGIN net +299 (run_invocation 177 + dock 47 + cards 32 + client 26 + ws_bridge 17); SERVER +134 (server.py handler + dispatch branch + __all__). Tests: plugin test_run_invocation.py 173 (22 green) + test_client dev-tool-invoke E2E +41 (stub +78) all green; server test_dev_tool_invoke_handler.py 203 (7 green). Live-drive driver headless_run_invocation_proof.py validated OFFLINE against stub (all 4 proofs green). Plugin 0.3.3->0.3.4, repackaged, running daemon serves 0.3.4 live (per-request). Offline suite baseline preserved 9-by-SET. LIVE daemon proof BLOCKED: new server.py needs one daemon restart (kickoff said do-not-restart) -- driver ready. |

| 2026-08-04m | 83 | 2 | 95 | ~38,000 | ADR 0119 charts window (plugin): in-chat ChartsPanel deleted (~205 LOC) -> bottom-docked TUFLOW-style ChartsWindow (list strip + canvas + nav toolbar + hover/click-inspect/wheel-zoom + Locate-on-map); render_spec byte-identical; plugin 14,194 -> 14,640 (+446); registry/coded UNCHANGED by the plugin wave (the SCHISM landing row 2026-08-04i carries registry 176 / coded 83). Plugin 0.3.5 -> 0.3.6 covers BOTH waves. |
| 2026-08-04n | +2 tools | +1 test file (test_swmm_network_import.py, 292) | +swmm_contracts.py (+83: SWMMNetworkLayerURI + SWMMDualDrainageLayerURI) | +0 (in-process engine core, no worker/image) | - | - | 181 -> 183 (+2) | - | ADR 0124 SWMM REAL-NETWORK FAMILY (rows #1+#2 LANDED). Registry 181 -> 183, CODED tools +2 (`swmm_network_import` + `swmm_dual_drainage_coupling`, both hand-written `@register_tool` engine="swmm" tier="template"). NEW engine core `agent/mesh/swmm_network.py` (+1335, SIBLING to raster_cell_mesh.py; NO edit to raster_cell_mesh/urban_flood/any flood-SFINCS seam -- grep-verified): parse_network_features (alias-aware field resolver + conduit-graph topology w/ endpoint-snap + node synthesis + labeled-degrade invert/diameter fills + SWMM one-link-outfall rule) -> build_network_inp (JUNCTIONS/OUTFALLS/CONDUITS + design-storm loading) -> run_network_deck (one-shot swmm5_run + Flow-Routing-Continuity honesty gate) -> network_to_geojson_4326; PLUS build_dual_drainage_inp (merge overland mesh deck + pipe network, one inlet orifice per pipe-junction->overland-cell; REUSES run_swmm_deck + postprocess_swmm VERBATIM) + read_network_response + dual_drainage_network_to_geojson_4326. Composers: workflows/swmm/network_import/ (+639, multi-source input: inline/s3/file/https-GeoJSON/keyless-ArcGIS-FeatureServer) + workflows/swmm/dual_drainage/ (+405). LIVE SMOKES (real public Houston-area TX Storm_Sewer_System FeatureServer, keyless, + MinIO): row#1 185 manholes+518 mains -> 484 junctions/473 conduits/47.3km pipe -> peak outfall 2.906 CMS, 464 flooded, 472 surcharged, continuity +0.482%, layer s3://trid3nt-runs/<rid>/network.geojson; row#2 overland depth 0.246m/0.0188km2 + 27 inlets coupled + 34 pipe conduits surcharged (real DEM-covered ~1km AOI), depth COG published. Retrieval: both tools top-8 for all corpus queries (model-free retrieve_visible_tools). Hygiene lint PASS. Offline: baseline EXACTLY 9-by-SET confirmed (fetch_resolution_gate x4 + run_river_dye_scenario x5, ran directly); full suite reached 87% clean before the 10-min foreground cap; 60 SWMM-surface tests + 8 new network tests + 58 swmm-contract tests all green; both tools register + server imports clean. ROWS #3-#7 STOPPED (published-deck-runner is a separate capability: LID/storage-curve/regulator/pump/RTC decks are pre-built published .inp, not GIS/DEM-synthesized, do not consume row-#1 machinery). |
| 2026-08-04o | +1 tool (levee_breach.py 616 + corpus.yaml 15; postprocess_hecras allow_dry path + run_hecras 2nd solver name, both shared) | +test_hecras_landing.py (+3 cases: levee archetype round-trip + dry-success layer + allow_dry read) + test_deck_edit.py (+9 breach-toggle cases) | +hecras_contracts.py (+36: muncie_levee_breach archetype literal + breach_enabled knob + HecrasDepthLayerURI.breach_enabled) | deck_edit.py +79 (set_breach_enabled) + entrypoint.py +45 (breach toggle + baked levee deck) | - | - | 187 -> 188 (+1) | - | ADR 0125 HEC-RAS LEVEE-BREACH archetype (engine #11 second archetype, template-first). Registry 187 -> 188, CODED tools +1 (`hecras_levee_breach`, hand-written `@register_tool` engine="hecras" tier="template"). SECOND archetype on the SAME frozen Muncie White River geometry (its 2D Interior Area is a LEVEED protected floodplain; the shipped `.bNN` carries a Breach Data block with 2 lateral-structure breaches). `set_breach_enabled` is the deterministic fixed-field toggle (breach-count->0 AND drop the record lines -- the empirically-pinned valid edit: leaving the records with a 0 count CRASHES RasUnsteady in Unetreal.for; dropping them is the clean levee-holds solve), composes with `scale_flow_hydrograph` (disjoint blocks). postprocess_hecras gains `allow_dry`: a 0-wet-cell solve is a VALID DRY SUCCESS (all-nodata depth COG + zeroed stats + breach_enabled=False + "LEVEE HELD" layer name), never HECRAS_OUTPUT_EMPTY. Depth raster HONESTLY REUSES continuous_flood_depth. Demonstration-geometry honesty LOUD (docstring fidelity line + envelope fallback_note verbatim-class: v1 runs the Muncie leveed-floodplain geometry, the Bald Eagle multi-2D model awaits the Windows-Phase-1 unblock, ledgered; off-scope -> sfincs_flood). ACCEPTANCE GREEN through the REGISTERED TEMPLATE (image rebuilt, real RasGeomPreprocess+RasUnsteady, MinIO): breach ON -> 4881 wet cells / depth_max 20.236 ft / depth_mean 7.47 ft / vol_err 0.005834%; breach OFF -> 0 wet cells / valid dry success / vol_err 0.002150% (both bbox-matched on the same 5765-cell 2D Interior Area). Both depth COGs published (F33 overview auto-translate) + breach-vs-holds comparison chart + on/off proof PNGs (scratchpad/levee_breach_proofs/). Retrieval: hecras_levee_breach top-8 for all 5 corpus queries (model-free retrieve_visible_tools). Daemon boot clean (registry 188). Image 2.2 GB unchanged (code layer only; fetch stage cached). Offline suite baseline preserved EXACTLY 9-by-SET across 4 foreground alphabetical slices (fetch_resolution x4 in [f-o] + river_dye x5 in [p-r]; [a-e] + [s-z] clean; 360 files, ~5811 passed). Two regressions from the new tool CAUGHT + FIXED (categories.py hazard_modeling mapping + test_door_dissolution EXPECTED_TEMPLATES 29->30). Flood/SFINCS seams untouched (grep-verified; the hecras acceptance IS this wave's canary). QUEUED (ledger): Bald Eagle Creek multi-2D levee (Windows-Phase-1 intermediates OR the neeraip-class third-party preprocessor decision -- NATE's call), rain-on-grid archetype (STOP: needs a shipped rain-on-grid tutorial geometry baked). |
