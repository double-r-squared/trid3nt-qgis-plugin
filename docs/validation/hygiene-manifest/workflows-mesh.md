# Hygiene manifest - `trid3nt_server/workflows/mesh`

Read-only lens. Every tracked file in the scope was opened end to end by the
agent that wrote its row; the greps below the tables are sweep guards run after
the read, never the census. `__pycache__` is untracked and out of scope.

Pure LOC is `scripts/loc_report.py`'s measure with one correction: the
instrument subtracts blank lines and docstring lines independently, so a blank
line INSIDE a docstring is subtracted twice. The `pure LOC` column is the
corrected count; `loc_report` is what the instrument prints today. Over this
scope the defect understates pure code by 240 lines (4,114 corrected against
3,874 reported), and it drives a docstring-only module negative (`-1`).

## Code files

| path | pure LOC (loc_report) | docstrings: count / lines / over-limit (fn+class >3, module >5) / disallowed classes present | comment blocks > 6 lines | history markers | refs to deleted or moved modules | fate | notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `__init__.py` | 0 (-1) | 1 / 7 / module 7 / rationale, architecture | none | 0 | none | TRIM | Whole docstring is why nothing is re-exported plus the router/registry/template import cycle - rationale and neighbour references. The one constraint (nothing is re-exported) is one line. |
| `artifact.py` | 143 (129) | 10 / 80 / module 26; `MeshArtifact` 10, `unsolvable_reason` 11, `find_case_mesh_artifacts` 7, `measured_min_edge_m` 6, `sidecar_key_for_mesh_uri` 5, `write_mesh_artifact_sidecar` 5, `read_mesh_artifact_sidecar` 5, `stash_mesh_artifact` 4 / architecture, rationale, narrative, roll-call | none | 0 | names ``publish_layer``'s `_LAST_LEGEND_BY_URI` - the stash is module-scope in `emission/publish.py`, not an attribute of `publish_layer` | TRIM | Module docstring is a two-seam architecture essay. `MeshArtifact` is a per-field roll-call of a typed dataclass. `unsolvable_reason` restates the `Accepts` seam that lives in `tool.py`. Nine `#:` field comments are the same roll-call in comment form; they are under 6 lines each and are constraints where they name what `None` means. |
| `gate.py` | 331 (321) | 14 / 63 / module 22; `_mesh_param_sheet` 7, `gate_mesh_build` 6, `_apply_gate_revision` 6, `active_mesh_session` 5, `open_mesh_gate` 5, `present_mesh` 5 / rationale, architecture, narrative | none | 0 | none | TRIM | The three `__doc__`-assigned gate tools (`mesh_accept` 382, `mesh_reset` 316, `mesh_adopt_layer` 404 chars) are LLM-facing and well inside the 1,000-char front budget - leave them. Module docstring's "ONE CARD PATH" paragraph is design rationale; the card's actual constraint (only `resolution_m` and `reset` are movable) is already enforced in `_apply_gate_revision`. |
| `grid_geometry.py` | 68 (63) | 3 / 26 / module 9, `regular_grid_from_bbox` 10, `RegularGrid` 7 / rationale, architecture, roll-call | none | 0 | none | TRIM | `regular_grid_from_bbox` carries a real input contract (bbox order, CRS, what raises) - trims to 3 with the equirectangular derivation moving to a comment block at the `m_per_deg_lon` line. `RegularGrid` is a field roll-call over `#:`-commented fields. |
| `inputs.py` | 67 (62) | 4 / 29 / module 14, `op_geometry` 8, `op_raster` 6 / narrative, rationale, architecture | none | 0 | none | TRIM | Module docstring's two-conversion table is the contract and survives as a comment block; the `artifact_class` neighbour reference and the "what a chain produced" narrative go. |
| `kinds.py` | 4 (2) | 1 / 11 / module 11 / rationale, architecture | none | 0 | none | TRIM | Four pure-code lines carrying an eleven-line essay about `Accepts` rows and autocomplete. The vocabulary itself is one `Literal`; the `#:` line above it already says what it is. |
| `op_tool.py` | 55 (50) | 3 / 42 / module 11, `mesh_op` 30, `_entry` 1 / architecture, rationale | none | 0 | none | TRIM | `mesh_op` is LLM-facing and runs **1,728 chars** - over the 1,000-char front budget, one of the ruled 19. Module docstring restates the declaration/runtime split that `meshers/__init__.mesh_op` also states. |
| `recipe.py` | 157 (147) | 13 / 59 / module 17; `recipe_plan_value` 7, `recipe_from_plan_value` 7, `MeshRecipe` 6, `_thaw` 6, `build_recipe` 5, `with_params` 5 / rationale, narrative, architecture | none | 0 | none | TRIM | The `mesh state = recipe + staged inputs` block is a design statement, not a caller contract. `with_params`'s "three agnostic params" is enforced in the body three lines below. |
| `session.py` | 362 (344) | 23 / 112 / module 15; `recipe_lines` 11, `accept` 10, `_display_face` 8, `_telemac_pair` 7, `replay_recipe` 7, `adopt_layer` 6, `_area_km2` 6, `snapshot` 6, `_synthetic_inputs` 6, `_staged_files` 6, `reset` 5, `_metre_scale` 5, `_stage` 5 / rationale, narrative, architecture | none | 0 | none | TRIM | `_display_face`'s "not a cache of STATE" paragraph is a memoization derivation - becomes a comment block at the `self._display is None` test. `accept`'s bed-provenance paragraph duplicates the two inline comment blocks already inside `accept`. |
| `shoreline.py` | 171 (167) | 10 / 40 / module 23, `land_polygons` 9 / narrative, rationale, roll-call | none | 0 | none | TRIM | The module's per-rung roll-call duplicates the `_OSM_TOOL` / `_GSHHG_NOMINAL_M` `#:` comments and the README's ladder table - one of the three should hold it. `land_polygons`'s land-on-the-left rule IS the constraint and belongs as a comment block at the `left = ...` line. |
| `step.py` | 64 (58) | 6 / 41 / module 12, `build_declared_mesh` 14, `build` 8, `_session_name` 5 / rationale, architecture, narrative | none | 0 | none | TRIM | "Restated and not repeated: a per-domain wrapper around this would be..." is a rejected-alternative argument - the disallowed rationale class exactly. |
| `tool.py` | 219 (205) | 13 / 109 / module 11; `build_mesh` 47, `resolve_mesh` 16, `accepts_for` 9, `supplied_mesh_artifact` 8, `kind_accepted` 7, `mesh_kind` 5 / architecture, rationale, narrative | none | 0 | none | TRIM | `build_mesh` is LLM-facing and runs **2,810 chars** - the largest overrun in the scope and one of the ruled 19. Its Params block and the two-mesher roster are the routing payload; the "Mesh creation is an EXPLICIT act" paragraph and the resolution-order narrative are not. `accepts_for`'s "THE REGISTRY IS THE ONE HOME" is architecture. |
| `topology.py` | 45 (40) | 3 / 33 / module 18, `read_topology` 14, `write_topology` 1 / rationale, architecture, **history** | none | 0 (see notes) | none | TRIM | The one history-class residue in the scope: `read_topology` explains the refusal by "It was numbered by the superseded row-order rule, which disagrees with the engine's own numbering..." - a statement about a replaced rule. The refusal's own message already carries the same sentence to the caller, so the docstring copy goes and the constraint (a bundle without per-boundary prescriptions is unrepairable) stays in one line. The two `#:` role constants carry genuine cross-file constraints and stay. |
| `meshers/__init__.py` | 286 (264) | 26 / 144 / module 17; `Mesh` 13, `Mesher` 12, `_edge_resolution_specs` 9, `OpNamespace` 8, `bind_ops` 8, `is_late_bound` 7, `MeshOp` 6, `mesh_op` 6, `BoundOp` 6, `resolve_op` 6, `roster` 6, `input_digest` 6, `MeshToolError` 6, `_field_of` 5, `fetch_activation_rows` 5, `_shared_primitives` 5, `register_mesher` 5 / architecture, rationale, roll-call | none | 0 | none | TRIM | Highest docstring density in the scope (144 docstring lines over 286 pure). `Mesh` and `Mesher` are field roll-calls; the `Mesh` docstring's "engine bundle" paragraph is a genuine contract (both arrays `None` means the engine realizes them) and survives as one line plus a comment block at `has_cells`. |
| `meshers/om2d.py` | 518 (501) | 25 / 126 / module 28; `_resampleable_lines` 13, `_clean_once` 11, `_emitted` 9, `_apply_tail` 8, `_staged` 8, `_lonlat_bounds` 8, `_open_sections` 8, `_refusal` 7, `_Domain` 6, `_conformal_probe` 6 / narrative, rationale, architecture | none | 0 | none | TRIM | The module's four-heading essay (NAMESPACES / ROLE ADAPTER / DEFAULT RECIPE) restates `meshers/__init__`'s module docstring. "an adapter that grows opinions is the old sin" is rationale with a backward glance. `_clean_once`'s single-precision derivation and `_resampleable_lines`'s below-the-edge derivation are real constraints and become comment blocks at the fusion tolerance and the length filter. The 8-line `deterministic=True` comment at the registration is a measured claim and stays (6 lines, under the block threshold anyway). |
| `meshers/reg_grid.py` | 59 (56) | 3 / 19 / module 13, `_with_ops` 5, `build` 1 / rationale, architecture | none | 0 | none | TRIM | "CONFORMS to the same surface every mesher does, with the smallest possible registration" is architecture. The zero-bed constraint (a zero-filled bed reads as ground at sea level) is real and moves to a comment block at `bed=None` in `_lattice`. |
| `meshers/drivers/__init__.py` | 5 (3) | 2 / 11 / module 10, `drivers_dir` 1 / architecture, rationale | none | 0 | none | TRIM | Five pure-code lines under a ten-line module docstring naming two images and explaining why the files are not in a sandbox. The mount contract (`drivers_dir()` is what a caller mounts as `/drivers`) is the keeper. |
| `meshers/drivers/om2d_driver.py` | 621 (591) | 34 / 222 / module 40; `set_rim_size` 26, `_rim_edges` 15, `_over_components` 15, `_seed_library_randomness` 12, `_walk_from` 9, `_contour_starts` 9, `_sections` 9, `_run_mesh_ops` 8, `_Build` 7, `_PolygonDomain` 7, `set_obstacle` 7, `pfix` 7, `_bind` 7, `_Refusal` 7, `guard` 6, `set_region_size` 6, `_components` 5, `_Holes` 5 / rationale, narrative, architecture | none | 0 | none | TRIM | Largest file and largest docstring load in the scope (222 lines). The module docstring's argv/config-key block IS a host-container contract that no signature carries - it belongs as a comment block at `main()` rather than as a 40-line module docstring or a ledger exemption. `set_rim_size`'s 26 lines are three separate derivations (why nothing else sizes the rim, what `constrain` buys, what `tolerance` measures) plus ORDER advice - each becomes a comment block at the line it governs. `_seed_library_randomness` and `_walk_from` document monkey-patches of library internals; those derivations are load-bearing and stay as comment blocks at the patch. |
| `meshers/drivers/selafin_cli_driver.py` | 250 (232) | 12 / 114 / module 30; `_liquid_boundaries` 15, `_prescribes` 12, `_refuse_shared_ring_nodes` 10, `_boundary` 10, `_successors` 7, `_joined` 7, `_roles_by_node` 6, `_Refusal` 6, `_contour_runs` 5, `_south_west` 5 / rationale, architecture, narrative | 1 block, lines 47-62 (16 lines, `_ROLE_CODES`) - **constraint** (the one authoring decision for the pair; uncapped by the ruling, keep) | 0 | none | TRIM | `_liquid_boundaries` is a port of `bief/front2.f`; its 15-line derivation is exactly the "derivation that governs a line of code" case and becomes a comment block at the walk. Duplicates `FREE_EXIT_ROLE` / `RATING_CURVE_ROLE` with `topology.py` by design (the container imports nothing from the tree) and `tests/test_telemac_boundary_contract.py` asserts the two agree - documentation only, no change. |
| `meshers/drivers/telemac_cas_driver.py` | 58 (52) | 6 / 40 / module 32 / rationale, narrative, architecture | none | 0 | none | TRIM | Fifty-eight pure lines under a 32-line module docstring. The argv/config contract and the three measured DAMOCLES caveats (FILE via `values`, `_EngineString` repr, `_EngineLogical` repr) are constraints; they belong at `main()` and at the two class bodies as comment blocks. Not a mesher driver: it drives the TELEMAC engine's steering reader for `workflows/telemac/authoring/cas_validate.py`. |
| `meshers/drivers/telemac_dico_driver.py` | 94 (88) | 6 / 37 / module 20, `de_latex` 7, `identifiers` 7 / rationale, narrative | none | 0 | none | TRIM | Its only non-test consumer is `scripts/extract_telemac_catalog.py` (the catalog extractor SCRIPTS RULED moves to `scripts/instruments/`); listed in `docs/validation/code-graph/orphans.md` for that reason. Not a mesher driver. The apostrophe/double-quote swap in `de_latex` is a real constraint and survives as a comment block. |
| `meshers/drivers/telemac_result_driver.py` | 41 (37) | 2 / 22 / module 21, `read_result` 1 / rationale, architecture, narrative | none | 0 | none | TRIM | Forty-one pure lines under a 21-line module docstring. The emitted-file shape (`(frames, nodes)` per `v<i>`, the 2D shape riding beside the 3D quantities) is the contract; the "which quantities are read and why" paragraph is rationale. Not a mesher driver: it serves `workflows/telemac/products/result_reader.py`. |
| `shared/__init__.py` | 0 (-1) | 1 / 7 / module 7 / rationale, architecture | none | 0 | none | TRIM | Zero pure lines. Two of the seven are the "so a second mesher reads the same writer rather than a copy of it" argument. |
| `shared/nodes.py` | 177 (166) | 11 / 78 / module 9; `sample_raster_at_nodes` 18, `read_centerline_utm` 12, `read_accepted_mesh_nodes` 8, `node_slopes_from_mesh` 8, `reproject_nodes_to_utm` 7, `read_2dm_mesh` 7, `_bilinear` 6 / rationale, narrative, architecture | none | 0 | **`_TIN_FORMATS = "scripts/sandbox/oceanmesh"`** and the `sys.path` insert in `tin_formats()` - `mesh_formats` is a module SCRIPTS RULED moves into `trid3nt_server/workflows/mesh/shared/formats/`, with the hack named to die | TRIM (blocked on the ruled move) | The ruled move lands a new `shared/formats/` package inside this scope; `tin_formats()`, `_TIN_FORMATS` and the `parents[4]` walk all go with it, and that is a code change outside this wave's documentation lane. `sample_raster_at_nodes` carries the longest non-LLM docstring in the scope: the rim-pixel derivation (two sides of a domain reading sea level) is a genuine constraint and becomes a comment block at the `np.clip` on `_RIM_PIXELS`. |
| `shared/primitives.py` | 265 (247) | 18 / 120 / module 17; `set_boundary_roles` 31, `set_bed` 18, `_window` 7, `_refuse_undated_source` 7, `_faces` 6, `_provenance` 6, `_runs` 6, `_bed_raster` 5, `_metre_nodes` 5, `_arc` 5 / rationale, narrative, architecture | none | 0 | none | TRIM, with `set_boundary_roles` an **EXEMPT candidate** | `set_boundary_roles` is the one docstring in the scope whose length is a contract rather than an essay: `**roles` accepts `{role: face}` or `{role: [face, ...]}`, a face may be a transect, a point or a ring, and the RUN-not-node-set semantics and the several-faces-one-role rule are what a caller gets wrong - none of it reachable from the signature. Proposed marker: `# docstring-exempt: **roles accepts four face shapes and a run-not-node-set contract the signature cannot carry`. `set_bed`'s 18 lines are the substitution policy (a DATA row's declaration, never this op's) - a comment block at the `TOOL_REGISTRY[name].fn` call. |
| `shared/selafin_cli.py` | 54 (51) | 3 / 24 / module 12, `write_telemac_pair` 11, `_run_driver` 1 / rationale, architecture | none | 0 | none | TRIM | The "two files are ONE artifact" statement is the constraint and appears three times in the scope (here, `session._telemac_pair`, and the driver's own module docstring); it should stand in one place. The `roles` vocabulary listed here duplicates `_ROLE_CODES` in the driver, which is where an unknown role actually refuses. |

Code-file totals: **26 files, 4,114 pure LOC (3,874 as `loc_report` prints it), 264 docstrings over 1,586 docstring lines, 1 comment block over 6 lines, 0 history markers, 1 reference to a module under a ruled move.**

## Docs and data files

| path | lines | what it is | stale claims | fate |
| --- | --- | --- | --- | --- |
| `README.md` | 56 | Directory map: two lead paragraphs, a 14-row file table, a prose section on the shoreline ladder with its own two-row table and an environment-variable note, and a two-row subfolder table. | (1) Every file it names exists and every tracked top-level file has a row - no dead names, no wrong counts. (2) The `session.py` row says session "REFUSES to freeze a boundary walk whose rings share a node"; the refusal is raised by `_refuse_shared_ring_nodes` inside `selafin_cli_driver.py` and re-raised through `session.accept` -> `_telemac_pair` - reachable but attributed to the wrong file. (3) The `drivers/` sentence says the folder "holds the in-container scripts mounted into the images whose libraries they drive"; three of its five files (`telemac_cas_driver`, `telemac_dico_driver`, `telemac_result_driver`) drive the TELEMAC ENGINE for consumers in `workflows/telemac/` and `scripts/`, not a mesher - the map does not say so. | REWRITE - trim to the ruled map shape (one paragraph + file table + subfolder table); the shoreline-ladder section and the `TRID3NT_GSHHG_SHP` note are longer-than-a-map material and move under `docs/`. Fix (2) and name the engine drivers in (3). |
| `corpus.yaml` | 61 | Retrieval phrasings for `build_mesh` (31) and `mesh_op` (22), walked into the main index by the `workflows/**/corpus.yaml` glob. | None. Every phrasing is a question class, not a place - no city, county or bbox appears; the one proper noun is an engine name (`TELEMAC`), which is vocabulary rather than a place. Both routed names are registered tools in this scope. | KEEP |

## Sweep guards run after the read

- History-marker classes (`job-N`, `sprint`, `ADR`, `wave N`, `milestone`, `FR-N`, person attribution, ISO dates, `TODO`/`FIXME`/`XXX`, `deprecated`/`legacy`/`formerly`/`renamed`): **0 hits** across the scope. Five `no longer` hits are all present-tense constraints about mesh state, not history.
- Named external symbols and tools resolved against the tree: `emission/mesh_display.{mesh_display_path,write_2dm}`, `emission/layer_uri_emit.publish_input_layer`, `emission/publish._LAST_LEGEND_BY_URI`, `workflows/solver/solver._get_s3_client`, `tools/fetchers/_router/registration.get_spec`, `tools/processing/_hydrology_common.write_conditioned_dem`, `workflows/runtime/{accepts,data,plan}`, `workflows/shared/geometry.{source_uri,utm_epsg_for,read_geometry_doc,flatten_geometries}`, `fetch_topobathy`, `fetch_osm_coastline`, `fetch_nhd_waterbodies`, `geocode_location` - **all present**.
- LLM-facing docstring front budget (1,000 chars): `build_mesh` **2,810**, `mesh_op` **1,728** - both over. The three `__doc__`-assigned gate tools are 382 / 316 / 404 - all under.
- Instrument defect found while measuring: `scripts/loc_report.py` subtracts blank lines and docstring lines independently, double-counting a blank line inside a docstring. Effect over this scope: 240 lines understated; a docstring-only module reports `-1` pure LOC.

files listed: 28 / files read: 28 / rows written: 28
