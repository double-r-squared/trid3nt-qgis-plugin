"""server.protocol package. Leaf modules are the module of record; the
trid3nt_server.server facade presents the flat namespace. Import leaves by
full path (e.g. trid3nt_server.server.protocol.<leaf>)."""
