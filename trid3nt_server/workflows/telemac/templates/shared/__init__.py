"""Shared template bodies: a PART a template lists, never a parent it extends.

A body here is named for what it IS, is created only when a good portion of a
template is shared, and has at least two users - one user folds back into its
own template. It carries the DATA rows and the MESH recipe it shares alongside
its slot assertions, because a chain that produces the same artifacts for the
same reason is as much the shared thing as a keyword is.
"""
