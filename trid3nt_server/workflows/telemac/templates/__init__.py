"""The TELEMAC templates: one package per question, over the module wrappers.

A template package is the recipe file, ``declarations.py`` and ``corpus.yaml``.
The recipe carries a STEERING body of raw keywords, the PARAMS it reads, the
DATA chain it consumes and the MESH recipe it triangulates on; ``shared/`` holds
the bodies and chains a good portion of several templates have in common.
"""
