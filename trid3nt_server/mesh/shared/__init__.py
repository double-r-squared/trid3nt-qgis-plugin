"""What every mesher needs and no mesher owns.

The per-solver geometry writers and the node primitives every mesher's output
goes through. They live here so the numbering agreement between a geometry and
the boundary file written against it is made ONCE."""
